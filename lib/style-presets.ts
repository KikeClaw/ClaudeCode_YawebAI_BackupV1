// Auto-generated by Opus 4.7 — do not edit manually.
// Run scripts/gen-design-systems.mjs to regenerate.

export type StylePresetKey =
  | 'auto'
  | 'clasico'
  | 'mediterraneo'
  | 'minimal'
  | 'vibrante'
  | 'lujo'
  | 'fresco'
  | 'urbano'
  | 'natural'
  | 'nordico'
  | 'festivo'
  | 'industrial'

export interface StylePreset {
  name: string
  description: string
  accentColor: string
  bgColor: string
  textColor: string
  /** Injected into the AI prompt as a design directive + HTML class contract */
  promptDirective: string
  /** CSS injected as <style id="yw-sp"> after generation — Opus-generated design system */
  cssInject: string
}

export const STYLE_PRESETS: Record<StylePresetKey, StylePreset> = {
  auto: {
    name: 'Auto',
    description: 'La IA elige el estilo según el negocio',
    accentColor: '#696cff',
    bgColor: '#f8f9fa',
    textColor: '#111827',
    promptDirective: '',
    cssInject: '',
  },

  clasico: {
    name: 'Clásico',
    description: 'Elegante, sobrio, profesional',
    accentColor: '#1a3a5c',
    bgColor: '#f8f7f4',
    textColor: '#1a1a1a',
    promptDirective: `Generate a complete, semantic HTML5 document for a Spanish local business website with the "Clásico" design system — timeless professional elegance suited for law firms, notaries, financial advisors, or consultancies. You MUST use the EXACT class names provided in the vocabulary: .nav, .nav-logo, .nav-links, .nav-cta, .nav-hamburger, .hero, .hero-inner, .hero-eyebrow, .hero-sub, .hero-btns, .btn-primary, .btn-secondary, .btn-outline, .section-heading, .section-sub, .about, .about-inner, .about-text, .about-img, .about-highlight, .cards-grid, .card, .card-icon, .card-title, .card-desc, .card-price, .feats-grid, .feat, .feat-icon, .feat-title, .feat-text, .reviews-grid, .review-card, .review-stars, .review-text, .review-author, .faq-list, .faq-item, .faq-q, .faq-a, .cta-band, .btn-cream, .contact-grid, .contact-info, .contact-form, .form-group, .form-submit, .footer-inner, .footer-top, .footer-brand, .footer-name, .footer-links, .footer-bottom, .fade-in, .map-embed. Do not invent alternative class names. Include in the <head> a Google Fonts <link> tag for Playfair Display (400, 700) and Inter (400, 500). Follow this palette strictly: navy #1a3a5c primary, gold #c9a96e accent, warm off-white #f8f7f4 background, body text #1a1a1a, surface white #ffffff, borders #e5e0d8. Use Playfair Display for all headings, Inter for body. Build sections in this order: sticky nav, hero with full-bleed image and centered serif headline, about with text + image, services (.cards-grid 3 columns), features (.feats-grid 2 columns), reviews (.reviews-grid 3 columns), FAQ using <details>/<summary>, CTA band, contact with form and map, footer. Alternate section backgrounds between #f8f7f4 and #ffffff. Content must be in natural Spanish, adapted to the business type, with professional, formal, trustworthy tone. Add class="fade-in" to major blocks for scroll animation.`,
    cssInject: `@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Inter:wght@400;500&display=swap');

:root {
  --primary: #1a3a5c;
  --primary-dark: #12293f;
  --accent: #c9a96e;
  --accent-dark: #b08f52;
  --bg: #f8f7f4;
  --text: #1a1a1a;
  --text-muted: #555;
  --surface: #ffffff;
  --border: #e5e0d8;
  --radius-card: 6px;
  --radius-btn: 6px;
  --shadow: 0 1px 6px rgba(0,0,0,.07);
  --shadow-hover: 0 6px 20px rgba(0,0,0,.10);
  --font-heading: 'Playfair Display', Georgia, serif;
  --font-body: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  --transition: all .3s ease;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

html { scroll-behavior: smooth; }

body {
  font-family: var(--font-body);
  font-weight: 400;
  color: var(--text);
  background: var(--bg);
  line-height: 1.7;
  -webkit-font-smoothing: antialiased;
}

h1, h2, h3, h4, h5 {
  font-family: var(--font-heading);
  font-weight: 700;
  color: var(--primary);
  line-height: 1.25;
  letter-spacing: -0.01em;
}

a { color: var(--primary); text-decoration: none; transition: var(--transition); }

img { max-width: 100%; display: block; }

section { padding: 110px 24px; }
section:nth-of-type(even) { background: var(--surface); }
section:nth-of-type(odd) { background: var(--bg); }

/* ---------- NAV ---------- */
.nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 22px 48px;
  background: transparent;
  transition: background .3s ease, padding .3s ease, box-shadow .3s ease;
}

.nav.scrolled {
  background: var(--surface);
  padding: 14px 48px;
  box-shadow: 0 1px 6px rgba(0,0,0,.06);
  border-bottom: 1px solid var(--border);
}

.nav-logo {
  font-family: var(--font-heading);
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--surface);
  letter-spacing: .02em;
}
.nav.scrolled .nav-logo { color: var(--primary); }

.nav-links {
  display: flex;
  list-style: none;
  gap: 40px;
  align-items: center;
}

.nav-links li a {
  font-size: .95rem;
  font-weight: 500;
  color: var(--surface);
  position: relative;
  padding-bottom: 4px;
}
.nav.scrolled .nav-links li a { color: var(--text); }

.nav-links li a::after {
  content: "";
  position: absolute;
  left: 0; bottom: 0;
  width: 0; height: 1px;
  background: var(--accent);
  transition: width .3s ease;
}
.nav-links li a:hover::after { width: 100%; }
.nav-links li a:hover { color: var(--accent); }
.nav.scrolled .nav-links li a:hover { color: var(--primary); }

.nav-cta {
  display: inline-block;
  padding: 10px 22px;
  background: var(--accent);
  color: var(--surface) !important;
  border-radius: var(--radius-btn);
  font-size: .9rem;
  font-weight: 500;
  letter-spacing: .02em;
  transition: var(--transition);
}
.nav-cta:hover { background: var(--accent-dark); transform: translateY(-1px); }
.nav-cta::after { display: none !important; }

.nav-hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  cursor: pointer;
  background: none;
  border: none;
  padding: 4px;
}
.nav-hamburger span {
  width: 26px; height: 2px;
  background: var(--surface);
  transition: var(--transition);
}
.nav.scrolled .nav-hamburger span { background: var(--primary); }

/* ---------- HERO ---------- */
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 120px 24px 80px;
  background: linear-gradient(rgba(26,58,92,.55), rgba(26,58,92,.55)),
              url('https://images.unsplash.com/photo-1589829545856-d10d557cf95f?w=1920') center/cover no-repeat;
  color: var(--surface);
  text-align: center;
}

.hero-inner {
  max-width: 880px;
  margin: 0 auto;
}

.hero-eyebrow {
  display: inline-block;
  font-size: .85rem;
  font-weight: 500;
  letter-spacing: .22em;
  text-transform: uppercase;
  color: var(--accent);
  margin-bottom: 24px;
}

.hero h1 {
  font-size: clamp(2.5rem, 5.5vw, 4.4rem);
  color: var(--surface);
  font-weight: 700;
  margin-bottom: 28px;
  line-height: 1.15;
}

.hero-sub {
  font-size: 1.15rem;
  font-weight: 400;
  color: rgba(255,255,255,.9);
  max-width: 640px;
  margin: 0 auto 40px;
  line-height: 1.7;
}

.hero-btns {
  display: flex;
  gap: 16px;
  justify-content: center;
  flex-wrap: wrap;
}

/* ---------- BUTTONS ---------- */
.btn-primary, .btn-secondary, .btn-outline, .btn-cream, .form-submit {
  display: inline-block;
  padding: 14px 32px;
  font-family: var(--font-body);
  font-size: .95rem;
  font-weight: 500;
  letter-spacing: .03em;
  border-radius: var(--radius-btn);
  border: 1px solid transparent;
  cursor: pointer;
  transition: var(--transition);
  text-align: center;
}

.btn-primary {
  background: var(--primary);
  color: var(--surface);
}
.btn-primary:hover { background: var(--primary-dark); color: var(--surface); transform: translateY(-2px); box-shadow: 0 6px 16px rgba(26,58,92,.25); }

.btn-secondary {
  background: var(--accent);
  color: var(--surface);
  animation: subtle-glow 3s ease-in-out infinite;
}
.btn-secondary:hover { background: var(--accent-dark); color: var(--surface); transform: translateY(-2px); box-shadow: 0 6px 16px rgba(201,169,110,.35); }

@keyframes subtle-glow {
  0%, 100% { box-shadow: 0 2px 8px rgba(201,169,110,.3); }
  50% { box-shadow: 0 4px 18px rgba(201,169,110,.5); }
}

.btn-outline {
  background: transparent;
  color: var(--surface);
  border: 1px solid var(--surface);
}
.btn-outline:hover { background: var(--surface); color: var(--primary); }

/* within light sections */
section .btn-outline { color: var(--primary); border-color: var(--primary); }
section .btn-outline:hover { background: var(--primary); color: var(--surface); }

/* ---------- SECTION HEADINGS ---------- */
.section-heading {
  text-align: center;
  margin-bottom: 70px;
}

.section-heading h2 {
  font-size: clamp(2rem, 3.5vw, 2.8rem);
  margin-bottom: 20px;
}

.section-heading::after {
  content: "";
  display: block;
  width: 60px; height: 1px;
  background: var(--accent);
  margin: 24px auto 0;
}

.section-sub {
  font-size: 1.05rem;
  color: var(--text-muted);
  max-width: 640px;
  margin: 20px auto 0;
  line-height: 1.7;
}

/* ---------- ABOUT ---------- */
.about-inner {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 80px;
  align-items: center;
}

.about-text h2 {
  font-size: clamp(1.8rem, 3vw, 2.5rem);
  margin-bottom: 28px;
  position: relative;
  padding-bottom: 20px;
}
.about-text h2::after {
  content: "";
  position: absolute;
  left: 0; bottom: 0;
  width: 60px; height: 1px;
  background: var(--accent);
}

.about-text p {
  color: var(--text-muted);
  margin-bottom: 18px;
  font-size: 1rem;
}

.about-highlight {
  display: inline-block;
  margin-top: 20px;
  padding: 16px 24px;
  border-left: 3px solid var(--accent);
  background: var(--bg);
  font-style: italic;
  color: var(--primary);
  font-family: var(--font-heading);
  font-size: 1.05rem;
}

.about-img {
  width: 100%;
  height: 520px;
  overflow: hidden;
  border-radius: var(--radius-card);
  box-shadow: var(--shadow);
}
.about-img img { width: 100%; height: 100%; object-fit: cover; display: block; }

/* ---------- CARDS / SERVICES ---------- */
.cards-grid {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
}

.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 40px 32px;
  box-shadow: var(--shadow);
  transition: var(--transition);
  position: relative;
}

.card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
  border-color: var(--accent);
}

.card-icon {
  width: 52px; height: 52px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: var(--bg);
  color: var(--accent);
  border-radius: 50%;
  font-size: 1.4rem;
  margin-bottom: 24px;
  border: 1px solid var(--border);
}

.card-title {
  font-size: 1.35rem;
  margin-bottom: 14px;
  color: var(--primary);
}

.card-desc {
  color: var(--text-muted);
  font-size: .95rem;
  margin-bottom: 20px;
  line-height: 1.7;
}

.card-price {
  display: inline-block;
  padding-top: 16px;
  border-top: 1px solid var(--border);
  font-family: var(--font-heading);
  font-size: 1.25rem;
  color: var(--accent);
  font-weight: 700;
  width: 100%;
}

/* ---------- FEATURES ---------- */
.feats-grid {
  max-width: 1100px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 40px;
}

.feat {
  display: flex;
  gap: 24px;
  padding: 28px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  transition: var(--transition);
}
.feat:hover { transform: translateY(-4px); box-shadow: var(--shadow-hover); }

.feat-icon {
  flex-shrink: 0;
  width: 48px; height: 48px;
  background: var(--primary);
  color: var(--accent);
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-card);
  font-size: 1.3rem;
}

.feat-title {
  font-size: 1.2rem;
  margin-bottom: 10px;
}

.feat-text {
  color: var(--text-muted);
  font-size: .95rem;
}

/* ---------- REVIEWS ---------- */
.reviews-grid {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
}

.review-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 36px 30px;
  box-shadow: var(--shadow);
  transition: var(--transition);
  position: relative;
}
.review-card:hover { transform: translateY(-4px); box-shadow: var(--shadow-hover); }

.review-card::before {
  content: "\\201C";
  position: absolute;
  top: 10px; right: 24px;
  font-family: var(--font-heading);
  font-size: 4rem;
  color: var(--accent);
  opacity: .25;
  line-height: 1;
}

.review-stars {
  color: var(--accent);
  font-size: 1rem;
  margin-bottom: 18px;
  letter-spacing: 2px;
}

.review-text {
  color: var(--text);
  font-style: italic;
  font-family: var(--font-heading);
  font-weight: 400;
  font-size: 1.02rem;
  line-height: 1.7;
  margin-bottom: 24px;
}

.review-author {
  font-size: .9rem;
  font-weight: 500;
  color: var(--primary);
  padding-top: 16px;
  border-top: 1px solid var(--border);
  letter-spacing: .03em;
}

/* ---------- FAQ ---------- */
.faq-list {
  max-width: 820px;
  margin: 0 auto;
}

.faq-item {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  margin-bottom: 14px;
  overflow: hidden;
  transition: var(--transition);
}
.faq-item[open] { border-color: var(--accent); box-shadow: var(--shadow); }

.faq-q {
  padding: 22px 28px;
  cursor: pointer;
  font-family: var(--font-heading);
  font-weight: 700;
  font-size: 1.08rem;
  color: var(--primary);
  list-style: none;
  display: flex;
  justify-content: space-between;
  align-items: center;
  transition: var(--transition);
}
.faq-q::-webkit-details-marker { display: none; }
.faq-q::after {
  content: "+";
  font-size: 1.5rem;
  color: var(--accent);
  transition: transform .3s ease;
  font-family: var(--font-body);
  font-weight: 400;
}
.faq-item[open] .faq-q::after { transform: rotate(45deg); }
.faq-q:hover { color: var(--accent); }

.faq-a {
  padding: 0 28px 22px;
  color: var(--text-muted);
  font-size: .98rem;
  line-height: 1.75;
}

/* ---------- CTA BAND ---------- */
.cta-band {
  background: var(--primary);
  color: var(--surface);
  text-align: center;
  padding: 100px 24px;
  position: relative;
}
.cta-band::before {
  content: "";
  display: block;
  width: 60px; height: 1px;
  background: var(--accent);
  margin: 0 auto 30px;
}

.cta-band h2 {
  color: var(--surface);
  font-size: clamp(1.9rem, 3.5vw, 2.8rem);
  margin-bottom: 20px;
}

.cta-band p {
  font-size: 1.1rem;
  color: rgba(255,255,255,.85);
  max-width: 620px;
  margin: 0 auto 36px;
}

.btn-cream {
  background: var(--accent);
  color: var(--surface);
  padding: 14px 34px;
  display: inline-block;
  border-radius: var(--radius-btn);
  font-weight: 500;
}
.btn-cream:hover { background: var(--surface); color: var(--primary); transform: translateY(-2px); }

/* ---------- CONTACT ---------- */
.contact-grid {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1.2fr;
  gap: 60px;
}

.contact-info h3 {
  font-size: 1.5rem;
  margin-bottom: 20px;
}
.contact-info p { color: var(--text-muted); margin-bottom: 14px; }
.contact-info strong { color: var(--primary); font-family: var(--font-heading); }

.contact-form {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 40px;
  box-shadow: var(--shadow);
}

.form-group { margin-bottom: 22px; }

.form-group label {
  display: block;
  font-size: .88rem;
  font-weight: 500;
  color: var(--primary);
  margin-bottom: 8px;
  letter-spacing: .02em;
}

.form-group input,
.form-group textarea {
  width: 100%;
  padding: 13px 16px;
  font-family: var(--font-body);
  font-size: .98rem;
  color: var(--text);
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: var(--radius-btn);
  transition: border-color .3s ease, background .3s ease, box-shadow .3s ease;
}

.form-group textarea { min-height: 140px; resize: vertical; }

.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--accent);
  background: var(--surface);
  box-shadow: 0 0 0 3px rgba(201,169,110,.15);
}

.form-submit {
  background: var(--primary);
  color: var(--surface);
  width: 100%;
  padding: 15px;
  border: none;
  font-weight: 500;
}
.form-submit:hover { background: var(--primary-dark); transform: translateY(-1px); }

.map-embed {
  width: 100%;
  height: 320px;
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  margin-top: 28px;
  filter: grayscale(30%);
}

/* ---------- FOOTER ---------- */
footer {
  background: #14304d;
  color: rgba(255,255,255,.75);
  padding: 70px 24px 24px;
}

.footer-inner { max-width: 1200px; margin: 0 auto; }

.footer-top {
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr 1fr;
  gap: 48px;
  padding-bottom: 40px;
  border-bottom: 1px solid rgba(255,255,255,.12);
}

.footer-brand p { font-size: .92rem; line-height: 1.7; margin-top: 14px; }

.footer-name {
  font-family: var(--font-heading);
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--surface);
  margin-bottom: 8px;
}

.footer-top h4 {
  color: var(--surface);
  font-size: 1rem;
  margin-bottom: 18px;
  font-family: var(--font-heading);
}

.footer-links {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.footer-links a {
  color: rgba(255,255,255,.7);
  font-size: .92rem;
  transition: var(--transition);
  position: relative;
}
.footer-links a:hover { color: var(--accent); padding-left: 6px; }

.footer-bottom {
  padding-top: 24px;
  text-align: center;
  font-size: .85rem;
  color: rgba(255,255,255,.55);
}

/* ---------- FADE-IN ---------- */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity .6s ease, transform .6s ease;
}
.fade-in.visible {
  opacity: 1;
  transform: none;
}

/* ---------- RESPONSIVE ---------- */
@media (max-width: 1024px) {
  .nav { padding: 18px 32px; }
  .nav.scrolled { padding: 12px 32px; }
  section { padding: 90px`,
  },

  mediterraneo: {
    name: 'Mediterráneo',
    description: 'Cálido, acogedor, artesanal',
    accentColor: '#c1440e',
    bgColor: '#faf6f0',
    textColor: '#2d1f13',
    promptDirective: `Generate a complete single-page HTML website for a Spanish local business using the Mediterráneo preset — a warm artisan hospitality aesthetic for restaurants, bakeries, rural hotels, tapas bars, and artisan shops. You MUST use the EXACT class names from this vocabulary (this is mandatory, no variations): .nav, .nav-logo, .nav-links, .nav-cta, .nav-hamburger, .hero, .hero-inner, .hero-eyebrow, .hero-sub, .hero-btns, .btn-primary, .btn-secondary, .btn-outline, .section-heading, .section-sub, .about, .about-inner, .about-text, .about-img, .about-highlight, .cards-grid, .card, .card-icon, .card-title, .card-desc, .card-price, .feats-grid, .feat, .feat-icon, .feat-title, .feat-text, .reviews-grid, .review-card, .review-stars, .review-text, .review-author, .faq-list, .faq-item, .faq-q, .faq-a, .cta-band, .btn-cream, .contact-grid, .contact-info, .contact-form, .form-group, .form-submit, .footer-inner, .footer-top, .footer-brand, .footer-name, .footer-links, .footer-bottom, .fade-in, .map-embed. Include in the <head>: <link href="https://fonts.googleapis.com/css2?family=Lora:wght@400;600&family=Inter:wght@400;500&display=swap" rel="stylesheet">. Follow this palette strictly: terracotta primary #c1440e, sandy accent #e8a87c, warm cream background #faf6f0, text #2d1f13, dark footer #1a0f08. Use Lora for all headings and Inter for body text. Cards must be white with rounded 16px corners and warm shadows; buttons must be fully pill-shaped (border-radius 50px) with generous padding; the hero must use a warm Mediterranean photo with an amber overlay and a centered Lora headline. Add SVG wave dividers between sections and alternate cream/white backgrounds. Write authentic Spanish copy that conveys family warmth, handcrafted quality, and southern European hospitality. Adapt naturally to the specific business type (menu items for restaurants, rooms for hotels, products for bakeries, etc.).`,
    cssInject: `@import url('https://fonts.googleapis.com/css2?family=Lora:wght@400;600&family=Inter:wght@400;500&display=swap');

:root {
  --primary: #c1440e;
  --primary-dark: #9a3609;
  --accent: #e8a87c;
  --accent-light: #f2c9a8;
  --bg: #faf6f0;
  --surface: #ffffff;
  --text: #2d1f13;
  --text-soft: #6b5445;
  --footer-bg: #1a0f08;
  --border: rgba(193, 68, 14, 0.08);
  --radius-card: 16px;
  --radius-btn: 50px;
  --shadow: 0 4px 24px rgba(193, 68, 14, 0.10);
  --shadow-hover: 0 12px 36px rgba(193, 68, 14, 0.18);
  --shadow-glow: 0 6px 20px rgba(193, 68, 14, 0.35);
  --font-heading: 'Lora', Georgia, serif;
  --font-body: 'Inter', -apple-system, sans-serif;
  --transition: 0.3s ease;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  font-family: var(--font-body);
  color: var(--text);
  background: var(--bg);
  line-height: 1.65;
  font-weight: 400;
  -webkit-font-smoothing: antialiased;
}

h1, h2, h3, h4 {
  font-family: var(--font-heading);
  font-weight: 600;
  line-height: 1.2;
  color: var(--text);
}

img { max-width: 100%; display: block; }
a { color: inherit; text-decoration: none; }

/* NAVIGATION */
.nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 6%;
  background: transparent;
  transition: all var(--transition);
}
.nav.scrolled {
  background: rgba(250, 246, 240, 0.96);
  backdrop-filter: blur(12px);
  box-shadow: 0 2px 16px rgba(193, 68, 14, 0.08);
  padding: 14px 6%;
}
.nav-logo {
  font-family: var(--font-heading);
  font-size: 1.5rem;
  font-weight: 600;
  color: var(--surface);
  letter-spacing: 0.3px;
}
.nav.scrolled .nav-logo { color: var(--primary); }
.nav-links {
  display: flex;
  gap: 36px;
  list-style: none;
  align-items: center;
}
.nav-links li a {
  font-size: 0.95rem;
  font-weight: 500;
  color: var(--surface);
  transition: color var(--transition);
  position: relative;
}
.nav.scrolled .nav-links li a { color: var(--text); }
.nav-links li a:hover { color: var(--accent); }
.nav.scrolled .nav-links li a:hover { color: var(--primary); }
.nav-links li a::after {
  content: '';
  position: absolute;
  bottom: -6px; left: 0;
  width: 0; height: 2px;
  background: var(--accent);
  transition: width var(--transition);
}
.nav-links li a:hover::after { width: 100%; }

.nav-cta {
  padding: 10px 24px;
  background: var(--primary);
  color: #fff !important;
  border-radius: var(--radius-btn);
  font-weight: 500;
  box-shadow: var(--shadow-glow);
  transition: all var(--transition);
}
.nav-cta:hover {
  background: var(--primary-dark);
  transform: translateY(-2px);
}
.nav-hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  cursor: pointer;
  background: none;
  border: none;
}
.nav-hamburger span {
  width: 26px; height: 2px;
  background: var(--surface);
  transition: all var(--transition);
}
.nav.scrolled .nav-hamburger span { background: var(--text); }

/* HERO */
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 120px 6% 80px;
  position: relative;
  background: linear-gradient(rgba(193, 68, 14, 0.40), rgba(26, 15, 8, 0.55)),
    url('https://images.unsplash.com/photo-1528605248644-14dd04022da1?w=1800') center/cover no-repeat;
  color: #fff;
}
.hero-inner { max-width: 860px; }
.hero-eyebrow {
  display: inline-block;
  font-size: 0.85rem;
  letter-spacing: 3px;
  text-transform: uppercase;
  padding: 8px 20px;
  border: 1px solid rgba(255,255,255,0.5);
  border-radius: var(--radius-btn);
  margin-bottom: 24px;
  font-weight: 500;
}
.hero h1 {
  font-size: clamp(2.4rem, 5.5vw, 4.2rem);
  color: #fff;
  margin-bottom: 24px;
  font-weight: 600;
}
.hero-sub {
  font-size: 1.15rem;
  max-width: 620px;
  margin: 0 auto 40px;
  opacity: 0.95;
  line-height: 1.7;
}
.hero-btns {
  display: flex;
  gap: 16px;
  justify-content: center;
  flex-wrap: wrap;
}

/* BUTTONS */
.btn-primary, .btn-secondary, .btn-outline, .btn-cream {
  display: inline-block;
  padding: 14px 32px;
  border-radius: var(--radius-btn);
  font-family: var(--font-body);
  font-weight: 500;
  font-size: 1rem;
  cursor: pointer;
  border: none;
  transition: all var(--transition);
  text-align: center;
}
.btn-primary {
  background: var(--primary);
  color: #fff;
  box-shadow: var(--shadow-glow);
  animation: warmPulse 2.8s ease-in-out infinite;
}
.btn-primary:hover {
  background: var(--primary-dark);
  transform: translateY(-3px);
  box-shadow: 0 10px 28px rgba(193, 68, 14, 0.45);
}
@keyframes warmPulse {
  0%, 100% { box-shadow: 0 6px 20px rgba(193, 68, 14, 0.35); }
  50% { box-shadow: 0 6px 28px rgba(232, 168, 124, 0.55); }
}
.btn-secondary {
  background: var(--accent);
  color: var(--text);
}
.btn-secondary:hover {
  background: var(--accent-light);
  transform: translateY(-3px);
}
.btn-outline {
  background: transparent;
  color: #fff;
  border: 1.5px solid rgba(255,255,255,0.8);
}
.btn-outline:hover {
  background: #fff;
  color: var(--primary);
  transform: translateY(-3px);
}

/* SECTIONS */
section { padding: 100px 6%; position: relative; }
section:nth-of-type(even) { background: var(--surface); }

.section-heading {
  text-align: center;
  margin-bottom: 16px;
}
.section-heading h2 {
  font-size: clamp(2rem, 3.5vw, 2.8rem);
  color: var(--text);
}
.section-sub {
  text-align: center;
  color: var(--text-soft);
  font-size: 1.05rem;
  max-width: 620px;
  margin: 0 auto 60px;
  line-height: 1.7;
}

/* ABOUT */
.about-inner {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 70px;
  align-items: center;
  max-width: 1200px;
  margin: 0 auto;
}
.about-text h2 {
  font-size: clamp(1.9rem, 3vw, 2.6rem);
  margin-bottom: 20px;
}
.about-text p {
  color: var(--text-soft);
  margin-bottom: 18px;
  line-height: 1.8;
}
.about-img {
  border-radius: var(--radius-card);
  overflow: hidden;
  box-shadow: var(--shadow);
}
.about-img img {
  width: 100%;
  height: 480px;
  object-fit: cover;
  transition: transform 0.8s ease;
}
.about-img:hover img { transform: scale(1.05); }
.about-highlight {
  display: inline-block;
  padding: 12px 22px;
  background: rgba(232, 168, 124, 0.18);
  border-left: 3px solid var(--accent);
  border-radius: 8px;
  color: var(--primary);
  font-weight: 500;
  margin-top: 20px;
  font-style: italic;
}

/* CARDS GRID */
.cards-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
  max-width: 1200px;
  margin: 0 auto;
}
.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 36px 30px;
  box-shadow: var(--shadow);
  transition: all var(--transition);
  text-align: center;
}
.card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
  border-color: rgba(193, 68, 14, 0.2);
}
.card-icon {
  width: 64px; height: 64px;
  display: flex; align-items: center; justify-content: center;
  margin: 0 auto 22px;
  background: rgba(193, 68, 14, 0.08);
  color: var(--primary);
  border-radius: 50%;
  font-size: 1.8rem;
  transition: all var(--transition);
}
.card:hover .card-icon {
  background: var(--accent);
  color: #fff;
  transform: rotate(-6deg);
}
.card-title {
  font-size: 1.35rem;
  margin-bottom: 12px;
}
.card-desc {
  color: var(--text-soft);
  font-size: 0.98rem;
  line-height: 1.7;
  margin-bottom: 18px;
}
.card-price {
  display: inline-block;
  font-family: var(--font-heading);
  font-size: 1.4rem;
  color: var(--primary);
  font-weight: 600;
  padding-top: 14px;
  border-top: 1px dashed var(--border);
  width: 100%;
}

/* FEATURES */
.feats-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 36px;
  max-width: 1100px;
  margin: 0 auto;
}
.feat {
  display: flex;
  gap: 22px;
  padding: 28px;
  background: var(--surface);
  border-radius: var(--radius-card);
  border: 1px solid var(--border);
  transition: all var(--transition);
}
.feat:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow);
}
.feat-icon {
  flex-shrink: 0;
  width: 52px; height: 52px;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  color: #fff;
  border-radius: 12px;
  display: flex; align-items: center; justify-content: center;
  font-size: 1.4rem;
}
.feat-title {
  font-size: 1.2rem;
  margin-bottom: 8px;
}
.feat-text {
  color: var(--text-soft);
  font-size: 0.95rem;
  line-height: 1.7;
}

/* REVIEWS */
.reviews-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
  max-width: 1200px;
  margin: 0 auto;
}
.review-card {
  background: var(--surface);
  border-radius: var(--radius-card);
  padding: 32px 28px;
  box-shadow: var(--shadow);
  border: 1px solid var(--border);
  transition: all var(--transition);
  position: relative;
}
.review-card::before {
  content: '"';
  position: absolute;
  top: 10px; right: 24px;
  font-family: var(--font-heading);
  font-size: 4.5rem;
  color: var(--accent);
  opacity: 0.4;
  line-height: 1;
}
.review-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
}
.review-stars {
  color: #e8a84c;
  font-size: 1.05rem;
  margin-bottom: 14px;
  letter-spacing: 2px;
}
.review-text {
  color: var(--text-soft);
  font-style: italic;
  line-height: 1.8;
  margin-bottom: 20px;
}
.review-author {
  font-weight: 500;
  color: var(--primary);
  font-family: var(--font-heading);
}

/* FAQ */
.faq-list {
  max-width: 820px;
  margin: 0 auto;
}
.faq-item {
  background: var(--surface);
  border-radius: var(--radius-card);
  border: 1px solid var(--border);
  margin-bottom: 14px;
  overflow: hidden;
  transition: all var(--transition);
}
.faq-item[open] {
  box-shadow: var(--shadow);
  border-color: rgba(193, 68, 14, 0.18);
}
.faq-q {
  padding: 22px 28px;
  cursor: pointer;
  font-weight: 500;
  font-size: 1.05rem;
  list-style: none;
  display: flex;
  justify-content: space-between;
  align-items: center;
  color: var(--text);
  transition: color var(--transition);
}
.faq-q::-webkit-details-marker { display: none; }
.faq-q::after {
  content: '+';
  font-size: 1.6rem;
  color: var(--primary);
  transition: transform var(--transition);
}
.faq-item[open] .faq-q::after { transform: rotate(45deg); }
.faq-q:hover { color: var(--primary); }
.faq-a {
  padding: 0 28px 24px;
  color: var(--text-soft);
  line-height: 1.8;
}

/* CTA BAND */
.cta-band {
  background: linear-gradient(135deg, var(--primary) 0%, #d85a28 100%);
  color: #fff;
  text-align: center;
  padding: 90px 6%;
  border-radius: 0;
  position: relative;
  overflow: hidden;
}
.cta-band::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(circle at 20% 50%, rgba(232, 168, 124, 0.3), transparent 50%);
}
.cta-band h2 {
  font-size: clamp(1.9rem, 3.2vw, 2.6rem);
  color: #fff;
  margin-bottom: 16px;
  position: relative;
}
.cta-band p {
  font-size: 1.1rem;
  max-width: 600px;
  margin: 0 auto 32px;
  opacity: 0.95;
  position: relative;
}
.btn-cream {
  background: var(--bg);
  color: var(--primary);
  box-shadow: 0 4px 18px rgba(0,0,0,0.15);
  position: relative;
}
.btn-cream:hover {
  background: #fff;
  transform: translateY(-3px);
  box-shadow: 0 10px 26px rgba(0,0,0,0.25);
}

/* CONTACT */
.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1.2fr;
  gap: 50px;
  max-width: 1150px;
  margin: 0 auto;
}
.contact-info {
  background: var(--surface);
  padding: 38px;
  border-radius: var(--radius-card);
  border: 1px solid var(--border);
  box-shadow: var(--shadow);
}
.contact-info h3 {
  font-size: 1.4rem;
  margin-bottom: 20px;
  color: var(--primary);
}
.contact-info p {
  color: var(--text-soft);
  margin-bottom: 14px;
  display: flex;
  align-items: center;
  gap: 12px;
}
.contact-form {
  background: var(--surface);
  padding: 38px;
  border-radius: var(--radius-card);
  border: 1px solid var(--border);
  box-shadow: var(--shadow);
}
.form-group { margin-bottom: 20px; }
.form-group label {
  display: block;
  font-size: 0.9rem;
  font-weight: 500;
  margin-bottom: 8px;
  color: var(--text);
}
.form-group input,
.form-group textarea {
  width: 100%;
  padding: 13px 16px;
  border: 1.5px solid var(--border);
  border-radius: 10px;
  font-family: var(--font-body);
  font-size: 0.98rem;
  background: var(--bg);
  color: var(--text);
  transition: all var(--transition);
}
.form-group textarea { min-height: 130px; resize: vertical; }
.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--accent);
  background: #fff;
  box-shadow: 0 0 0 4px rgba(232, 168, 124, 0.2);
}
.form-submit {
  width: 100%;
  padding: 15px;
  background: var(--primary);
  color: #fff;
  border: none;
  border-radius: var(--radius-btn);
  font-family: var(--font-body);
  font-weight: 500;
  font-size: 1rem;
  cursor: pointer;
  box-shadow: var(--shadow-glow);
  transition: all var(--transition);
}
.form-submit:hover {
  background: var(--primary-dark);
  transform: translateY(-2px);
}
.form-submit:active { transform: translateY(0); }

.map-embed {
  width: 100%;
  height: 380px;
  border: none;
  border-radius: var(--radius-card);
  box-shadow: var(--shadow);
  margin-top: 40px;
  filter: sepia(0.15) saturate(1.05);
}

/* FOOTER */
footer {
  background: var(--footer-bg);
  color: #c9b8a8;
  padding: 70px 6% 30px;
}
.footer-inner { max-width: 1200px; margin: 0 auto; }
.footer-top {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr 1fr;
  gap: 50px;
  padding-bottom: 40px;
  border-bottom: 1px solid rgba(232, 168, 124, 0.15);
}
.footer-brand p {
  margin-top: 14px;
  opacity: 0.75;
  line-height: 1.7;
  font-size: 0.95rem;
}
.footer-name {
  font-family: var(--font-heading);
  font-size: 1.6rem;
  font-weight: 600;
  color: var(--accent);
}
.footer-links {
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.footer-links a {
  color: #c9b8a8;
  font-size: 0.95rem;
  transition: color var(--transition);
}
.footer-links a:hover { color: var(--accent); }
.footer-bottom {
  padding-top: 24px;
  text-align: center;
  font-size: 0.88rem;
  opacity: 0.6;
}

/* UTILITIES */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}
.fade-in.visible {
  opacity: 1;
  transform: none;
}

/* RESPONSIVE */
@media (max-width: 1024px) {
  .cards-grid { grid-template-columns: repeat(2, 1fr); }
  .reviews-grid { grid-template-columns: repeat(2, 1fr); }
  .footer-top { grid-template-columns: 1fr 1fr; gap: 36px; }
  .about-inner { gap: 40px; }
}

@media (max-width: 768px) {
  section { padding: 70px 5%; }
  .nav { padding: 16px 5%; }
  .nav-links {
    display: none;
    position: absol`,
  },

  minimal: {
    name: 'Minimal',
    description: 'Limpio, moderno, sin distracciones',
    accentColor: '#111111',
    bgColor: '#ffffff',
    textColor: '#111111',
    promptDirective: `Generate a complete, single-page Spanish local business website using pure editorial minimalism. You MUST use the EXACT class names from the provided vocabulary — .nav, .nav-logo, .nav-links, .nav-cta, .hero, .hero-inner, .hero-eyebrow, .hero-sub, .hero-btns, .btn-primary, .btn-secondary, .btn-outline, .section-heading, .section-sub, .about-inner, .about-text, .about-img, .about-highlight, .cards-grid, .card, .card-icon, .card-title, .card-desc, .card-price, .feats-grid, .feat, .feat-icon, .feat-title, .feat-text, .reviews-grid, .review-card, .review-stars, .review-text, .review-author, .faq-list, .faq-item, .faq-q, .faq-a, .cta-band, .contact-grid, .contact-info, .contact-form, .form-group, .form-submit, .footer-inner, .footer-top, .footer-brand, .footer-name, .footer-links, .footer-bottom, .fade-in, and .map-embed. Do not invent new class names. Include in the <head> this exact tag: <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;700;900&display=swap" rel="stylesheet">. Use strictly the palette: near-black #111111 text, pure white #ffffff backgrounds everywhere (no section color alternation), cool gray #f5f5f5 only for rare subtle surfaces, and #e5e5e5 for 1px dividers and borders. The hero must be TYPE-ONLY: a massive Inter 900 headline with negative letter-spacing filling the viewport — no gradients, no overlays, no decorative shapes. All sections share white backgrounds separated by thin 1px dividers with generous 140–160px vertical padding. Buttons are sharp, uppercase, letter-spaced. Cards are flat with hairline borders, never shadowed. Adapt all copy naturally in Spanish for the specific business type (salon, architect, studio, luxury retail, photography), maintaining a confident, restrained editorial tone. Every element must earn its place — whitespace is the primary design tool.`,
    cssInject: `@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;700;900&display=swap');

:root {
  --primary: #111111;
  --accent: #111111;
  --bg: #ffffff;
  --text: #111111;
  --text-muted: #666666;
  --surface: #f5f5f5;
  --border: #e5e5e5;
  --radius-card: 4px;
  --radius-btn: 4px;
  --shadow: none;
  --font-heading: 'Inter', sans-serif;
  --font-body: 'Inter', sans-serif;
  --section-pad: 150px;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: var(--font-body);
  font-weight: 400;
  line-height: 1.75;
  color: var(--text);
  background: var(--bg);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-heading);
  font-weight: 900;
  letter-spacing: -0.04em;
  line-height: 1.05;
  color: var(--text);
}

p {
  color: var(--text);
}

a {
  color: inherit;
  text-decoration: none;
  transition: color 0.2s ease, border-color 0.2s ease;
}

img {
  max-width: 100%;
  display: block;
}

/* NAV */
.nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24px 60px;
  background: transparent;
  transition: background 0.3s ease, border-color 0.3s ease, padding 0.3s ease;
  border-bottom: 1px solid transparent;
}

.nav.scrolled {
  background: #ffffff;
  border-bottom: 1px solid var(--border);
  padding: 18px 60px;
}

.nav-logo {
  font-family: var(--font-heading);
  font-weight: 900;
  font-size: 1.1rem;
  letter-spacing: -0.03em;
  text-transform: uppercase;
}

.nav-links {
  display: flex;
  list-style: none;
  gap: 40px;
  align-items: center;
}

.nav-links li a {
  font-size: 0.85rem;
  font-weight: 400;
  letter-spacing: 0.04em;
  color: var(--text);
  position: relative;
  padding: 4px 0;
  border-bottom: 1px solid transparent;
}

.nav-links li a:hover {
  border-bottom-color: var(--text);
}

.nav-cta {
  font-size: 0.8rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  padding: 12px 22px;
  background: var(--text);
  color: #fff;
  border: 1px solid var(--text);
  border-radius: var(--radius-btn);
  cursor: pointer;
  transition: all 0.25s ease;
}

.nav-cta:hover {
  background: #fff;
  color: var(--text);
}

.nav-hamburger {
  display: none;
  width: 28px;
  height: 20px;
  position: relative;
  cursor: pointer;
  background: none;
  border: none;
}

.nav-hamburger span {
  display: block;
  width: 100%;
  height: 1.5px;
  background: var(--text);
  margin-bottom: 6px;
}

/* HERO */
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 160px 60px 80px;
  background: #ffffff;
  border-bottom: 1px solid var(--border);
}

.hero-inner {
  max-width: 1400px;
  width: 100%;
  text-align: left;
}

.hero-eyebrow {
  font-size: 0.8rem;
  font-weight: 400;
  letter-spacing: 0.24em;
  text-transform: uppercase;
  color: var(--text-muted);
  margin-bottom: 40px;
}

.hero h1 {
  font-size: clamp(3rem, 10vw, 9rem);
  font-weight: 900;
  letter-spacing: -0.055em;
  line-height: 0.92;
  margin-bottom: 50px;
  max-width: 14ch;
}

.hero-sub {
  font-size: 1.15rem;
  font-weight: 300;
  line-height: 1.6;
  max-width: 560px;
  color: var(--text-muted);
  margin-bottom: 50px;
}

.hero-btns {
  display: flex;
  gap: 14px;
  flex-wrap: wrap;
}

/* BUTTONS */
.btn-primary,
.btn-secondary,
.btn-outline {
  display: inline-block;
  font-family: var(--font-body);
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  padding: 16px 30px;
  border-radius: var(--radius-btn);
  cursor: pointer;
  transition: all 0.25s ease;
  border: 1px solid var(--text);
}

.btn-primary {
  background: var(--text);
  color: #fff;
}

.btn-primary:hover {
  background: #fff;
  color: var(--text);
}

.btn-secondary {
  background: transparent;
  color: var(--text);
  border-color: var(--text);
}

.btn-secondary:hover {
  background: var(--text);
  color: #fff;
}

.btn-outline {
  background: transparent;
  color: var(--text);
  border-color: var(--border);
}

.btn-outline:hover {
  border-color: var(--text);
}

.btn-primary:focus,
.btn-secondary:focus,
.btn-outline:focus,
.nav-cta:focus {
  outline: 2px solid var(--text);
  outline-offset: 3px;
}

.btn-primary:active,
.btn-secondary:active,
.btn-outline:active {
  transform: translateY(1px);
}

/* SECTIONS */
section {
  padding: var(--section-pad) 60px;
  background: #ffffff;
  border-bottom: 1px solid var(--border);
}

.section-heading {
  max-width: 1400px;
  margin: 0 auto 80px;
}

.section-heading h2 {
  font-size: clamp(2rem, 5vw, 3.75rem);
  font-weight: 900;
  letter-spacing: -0.04em;
  line-height: 1;
  margin-bottom: 24px;
  max-width: 20ch;
}

.section-sub {
  font-size: 1.05rem;
  font-weight: 300;
  line-height: 1.7;
  color: var(--text-muted);
  max-width: 55ch;
}

/* ABOUT */
.about {
  padding: var(--section-pad) 60px;
}

.about-inner {
  max-width: 1400px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 100px;
  align-items: center;
}

.about-text h2 {
  font-size: clamp(2rem, 4.5vw, 3.5rem);
  letter-spacing: -0.04em;
  margin-bottom: 30px;
}

.about-text p {
  font-size: 1rem;
  line-height: 1.8;
  margin-bottom: 20px;
  color: var(--text);
}

.about-img {
  width: 100%;
  aspect-ratio: 4/5;
  overflow: hidden;
  border-radius: var(--radius-card);
  filter: grayscale(100%);
}

.about-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.about-highlight {
  display: inline-block;
  margin-top: 30px;
  padding: 20px 0;
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
  font-size: 0.85rem;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  font-weight: 700;
}

/* CARDS GRID — SERVICES */
.cards-grid {
  max-width: 1400px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0;
  border-top: 1px solid var(--border);
  border-left: 1px solid var(--border);
}

.card {
  background: #ffffff;
  border-right: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
  border-radius: 0;
  padding: 50px 40px;
  transition: background 0.3s ease, transform 0.3s ease;
  display: flex;
  flex-direction: column;
}

.card:hover {
  background: var(--surface);
  transform: translateY(-4px);
}

.card-icon {
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--text-muted);
  margin-bottom: 40px;
}

.card-title {
  font-size: 1.5rem;
  font-weight: 900;
  letter-spacing: -0.03em;
  margin-bottom: 16px;
}

.card-desc {
  font-size: 0.95rem;
  line-height: 1.7;
  color: var(--text-muted);
  margin-bottom: 30px;
  flex-grow: 1;
}

.card-price {
  font-size: 1rem;
  font-weight: 700;
  letter-spacing: 0.04em;
  padding-top: 24px;
  border-top: 1px solid var(--border);
}

/* FEATS GRID */
.feats-grid {
  max-width: 1400px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 80px 100px;
}

.feat {
  padding: 20px 0;
  border-top: 1px solid var(--border);
}

.feat-icon {
  font-size: 0.8rem;
  font-weight: 700;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--text-muted);
  margin-bottom: 24px;
}

.feat-title {
  font-size: 1.6rem;
  font-weight: 900;
  letter-spacing: -0.03em;
  margin-bottom: 16px;
}

.feat-text {
  font-size: 1rem;
  line-height: 1.75;
  color: var(--text-muted);
}

/* REVIEWS */
.reviews-grid {
  max-width: 1400px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 40px;
}

.review-card {
  background: #ffffff;
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 45px 35px;
  transition: transform 0.3s ease, border-color 0.3s ease;
}

.review-card:hover {
  transform: translateY(-4px);
  border-color: var(--text);
}

.review-stars {
  font-size: 0.8rem;
  letter-spacing: 0.3em;
  color: var(--text);
  margin-bottom: 28px;
}

.review-text {
  font-size: 1rem;
  line-height: 1.75;
  font-weight: 300;
  margin-bottom: 30px;
  color: var(--text);
}

.review-author {
  font-size: 0.8rem;
  font-weight: 700;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  padding-top: 20px;
  border-top: 1px solid var(--border);
}

/* FAQ */
.faq-list {
  max-width: 900px;
  margin: 0 auto;
}

.faq-item {
  border-bottom: 1px solid var(--border);
  padding: 8px 0;
}

.faq-item:first-child {
  border-top: 1px solid var(--border);
}

.faq-q {
  cursor: pointer;
  font-size: 1.15rem;
  font-weight: 700;
  letter-spacing: -0.02em;
  padding: 28px 40px 28px 0;
  position: relative;
  list-style: none;
  transition: color 0.2s ease;
}

.faq-q::-webkit-details-marker {
  display: none;
}

.faq-q::after {
  content: '+';
  position: absolute;
  right: 0;
  top: 50%;
  transform: translateY(-50%);
  font-size: 1.4rem;
  font-weight: 300;
  transition: transform 0.3s ease;
}

.faq-item[open] .faq-q::after {
  content: '–';
}

.faq-q:hover {
  color: var(--text-muted);
}

.faq-a {
  padding: 0 40px 28px 0;
  font-size: 1rem;
  line-height: 1.75;
  color: var(--text-muted);
  font-weight: 300;
}

/* CTA BAND */
.cta-band {
  background: var(--text);
  color: #fff;
  padding: 140px 60px;
  text-align: center;
  border-bottom: 1px solid var(--text);
}

.cta-band h2 {
  color: #fff;
  font-size: clamp(2rem, 5.5vw, 4.5rem);
  letter-spacing: -0.04em;
  line-height: 1;
  margin-bottom: 30px;
  max-width: 18ch;
  margin-left: auto;
  margin-right: auto;
}

.cta-band p {
  color: #bbb;
  font-size: 1.1rem;
  font-weight: 300;
  max-width: 50ch;
  margin: 0 auto 50px;
}

.cta-band .btn-cream {
  display: inline-block;
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  padding: 16px 36px;
  background: #fff;
  color: var(--text);
  border: 1px solid #fff;
  border-radius: var(--radius-btn);
  transition: all 0.25s ease;
}

.cta-band .btn-cream:hover {
  background: transparent;
  color: #fff;
}

/* CONTACT */
.contact-grid {
  max-width: 1400px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 100px;
}

.contact-info {
  font-size: 1rem;
  line-height: 1.9;
}

.contact-info h3 {
  font-size: 1.4rem;
  margin-bottom: 24px;
  letter-spacing: -0.02em;
}

.contact-info p {
  color: var(--text-muted);
  margin-bottom: 10px;
}

.contact-info a:hover {
  border-bottom: 1px solid var(--text);
}

.contact-form {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.form-group {
  display: flex;
  flex-direction: column;
}

.form-group label {
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  margin-bottom: 10px;
  color: var(--text);
}

.form-group input,
.form-group textarea {
  font-family: var(--font-body);
  font-size: 1rem;
  padding: 14px 0;
  border: none;
  border-bottom: 1px solid var(--border);
  background: transparent;
  color: var(--text);
  transition: border-color 0.25s ease;
  resize: vertical;
}

.form-group textarea {
  min-height: 120px;
}

.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-bottom-color: var(--accent);
}

.form-submit {
  align-self: flex-start;
  margin-top: 16px;
  font-family: var(--font-body);
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  padding: 16px 36px;
  background: var(--text);
  color: #fff;
  border: 1px solid var(--text);
  border-radius: var(--radius-btn);
  cursor: pointer;
  transition: all 0.25s ease;
}

.form-submit:hover {
  background: #fff;
  color: var(--text);
}

.map-embed {
  width: 100%;
  height: 420px;
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  filter: grayscale(100%) contrast(1.05);
  margin-top: 60px;
}

/* FOOTER */
footer {
  background: #ffffff;
  padding: 100px 60px 40px;
  border-top: 1px solid var(--border);
}

.footer-inner {
  max-width: 1400px;
  margin: 0 auto;
}

.footer-top {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr 1fr;
  gap: 60px;
  padding-bottom: 80px;
  border-bottom: 1px solid var(--border);
}

.footer-brand {
  max-width: 360px;
}

.footer-name {
  font-family: var(--font-heading);
  font-weight: 900;
  font-size: 1.3rem;
  letter-spacing: -0.03em;
  text-transform: uppercase;
  margin-bottom: 20px;
}

.footer-brand p {
  font-size: 0.95rem;
  color: var(--text-muted);
  line-height: 1.7;
}

.footer-links {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.footer-links a {
  font-size: 0.9rem;
  color: var(--text-muted);
  border-bottom: 1px solid transparent;
  align-self: flex-start;
}

.footer-links a:hover {
  color: var(--text);
  border-bottom-color: var(--text);
}

.footer-bottom {
  padding-top: 30px;
  font-size: 0.8rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--text-muted);
  display: flex;
  justify-content: space-between;
}

/* UTILITIES */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.fade-in.visible {
  opacity: 1;
  transform: none;
}

/* RESPONSIVE */
@media (max-width: 1024px) {
  .nav, .nav.scrolled { padding: 18px 40px; }
  section, .about, footer { padding-left: 40px; padding-right: 40px; }
  .hero { padding: 140px 40px 80px; }
  :root { --section-pad: 110px; }
  .cards-grid { grid-template-columns: repeat(2, 1fr); }
  .reviews-grid { grid-template-columns: repeat(2, 1fr); }
  .about-inner { gap: 60px; }
  .footer-top { grid-template-columns: 1fr 1fr; gap: 40px; }
}

@media (max-width: 768px) {
  .nav, .nav.scrolled { padding: 16px 24px; }
  .nav-links { display: none; }
  .nav-hamburger { display: block; }
  .nav-cta { display: none; }

  section, .about, footer { padding-left: 24px; padding-right: 24px; }
  :root { --section-pad: 80px; }

  .hero { padding: 120px 24px 60px; min-height: 90vh; }
  .hero h1 { font-size: clamp(2.5rem, 13vw, 5rem); }
  .hero-sub { font-size: 1rem; }
  .hero-btns { flex-direction: column; align-items: stretch; }
  .hero-btns .btn-primary,
  .hero-btns .btn-secondary { text-align: center; }

  .section-heading { margin-bottom: 50px; }
  .section-heading h2 { font-size: clamp(1.75rem, 8vw, 2.5rem); }

  .about-inner { grid-template-columns: 1fr; gap: 50px; }
  .cards-grid { grid-template-columns: 1fr; }
  .card { padding: 40px 28px; }
  .feats-grid { grid-template-columns: 1fr; gap: 50px; }
  .reviews-grid { grid-template-columns: 1fr; }
  .review-`,
  },

  vibrante: {
    name: 'Vibrante',
    description: 'Energético, atrevido, juvenil',
    accentColor: '#7c3aed',
    bgColor: '#fafafa',
    textColor: '#111111',
    promptDirective: `Generate a complete, single-file HTML website in Spanish for the specified local business using the "Vibrante" design system. This preset is built for high-energy brands (gyms, fitness studios, dance academies, sport brands, youth-oriented businesses) and communicates bold kinetic energy, forward momentum and zero-compromise ambition. You MUST include this exact Google Fonts link in the <head>: <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;800;900&display=swap" rel="stylesheet">. You MUST use the EXACT class names from the standardised vocabulary provided — no renaming, no substitutions, no additions with different names. This is mandatory because the CSS targets these hooks precisely. Structure the page as: sticky .nav (logo + links + CTA + hamburger), full-viewport .hero with oversized headline where ONE impactful word is wrapped in a span with amber accent color, about section with highlight callout, three-column .cards-grid of services with prices in amber, two-column .feats-grid, three-column .reviews-grid with star ratings, .faq-list using <details>/<summary>, a vivid purple .cta-band, two-column .contact-grid with form + info + .map-embed iframe, and a footer. Follow this color palette strictly: primary #7c3aed, secondary #9333ea, amber accent #f59e0b, background #fafafa, text #111111, with gradient linear-gradient(135deg,#7c3aed,#9333ea). Use Outfit weights 400/800/900, huge letter-spaced headlines, pill-shaped buttons, white cards with purple left-border accent and strong shadows. Add .fade-in to major blocks for scroll animation. Write authentic Spanish copy adapted to the business type — energetic, action-driven, using tú/vosotros tone, emphasising transformation, community and results. Include realistic prices, 5-6 services, 3-4 testimonials, and 4-6 FAQs.`,
    cssInject: `@import url('https://fonts.googleapis.com/css2?family=Outfit:wght@400;800;900&display=swap');

:root {
  --primary: #7c3aed;
  --primary-dark: #4c1d95;
  --secondary: #9333ea;
  --accent: #f59e0b;
  --bg: #fafafa;
  --surface: #ffffff;
  --text: #111111;
  --text-soft: #4b5563;
  --muted: #9ca3af;
  --radius-card: 20px;
  --radius-btn: 50px;
  --shadow: 0 8px 32px rgba(124, 58, 237, 0.18);
  --shadow-hover: 0 16px 48px rgba(124, 58, 237, 0.28);
  --gradient: linear-gradient(135deg, #7c3aed, #9333ea);
  --gradient-hero: linear-gradient(135deg, #7c3aed 0%, #4c1d95 100%);
  --font-heading: 'Outfit', sans-serif;
  --font-body: 'Outfit', sans-serif;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  font-family: var(--font-body);
  font-weight: 400;
  background: var(--bg);
  color: var(--text);
  line-height: 1.6;
  overflow-x: hidden;
}

h1, h2, h3, h4 {
  font-family: var(--font-heading);
  font-weight: 900;
  letter-spacing: -0.04em;
  line-height: 1.05;
}

img { max-width: 100%; display: block; }
a { color: inherit; text-decoration: none; }

/* NAV */
.nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.2rem 5vw;
  background: transparent;
  transition: all 0.35s ease;
}
.nav.scrolled {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(12px);
  box-shadow: 0 4px 20px rgba(124, 58, 237, 0.1);
  padding: 0.8rem 5vw;
}
.nav-logo {
  font-family: var(--font-heading);
  font-weight: 900;
  font-size: 1.5rem;
  letter-spacing: -0.03em;
  color: var(--text);
}
.nav.scrolled .nav-logo { color: var(--primary); }
.nav-links {
  display: flex;
  gap: 2.2rem;
  list-style: none;
}
.nav-links li a {
  font-weight: 800;
  font-size: 0.95rem;
  color: var(--text);
  position: relative;
  transition: color 0.25s;
}
.nav-links li a::after {
  content: '';
  position: absolute;
  left: 0; bottom: -6px;
  width: 0; height: 3px;
  background: var(--gradient);
  border-radius: 2px;
  transition: width 0.3s ease;
}
.nav-links li a:hover { color: var(--primary); }
.nav-links li a:hover::after { width: 100%; }
.nav-cta {
  background: var(--gradient);
  color: #fff !important;
  padding: 0.7rem 1.6rem;
  border-radius: var(--radius-btn);
  font-weight: 800;
  font-size: 0.9rem;
  box-shadow: 0 6px 20px rgba(124, 58, 237, 0.35);
  transition: transform 0.25s, box-shadow 0.25s;
}
.nav-cta:hover {
  transform: translateY(-2px) scale(1.04);
  box-shadow: 0 10px 28px rgba(124, 58, 237, 0.5);
}
.nav-hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  cursor: pointer;
  background: none;
  border: none;
}
.nav-hamburger span {
  width: 26px; height: 3px;
  background: var(--text);
  border-radius: 2px;
  transition: 0.3s;
}

/* HERO */
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--gradient-hero);
  color: #fff;
  padding: 7rem 5vw 6rem;
  position: relative;
  overflow: hidden;
  clip-path: polygon(0 0, 100% 0, 100% 97%, 0 100%);
}
.hero::before {
  content: '';
  position: absolute;
  top: -20%; right: -10%;
  width: 600px; height: 600px;
  background: radial-gradient(circle, rgba(245, 158, 11, 0.25), transparent 70%);
  border-radius: 50%;
  pointer-events: none;
}
.hero-inner {
  max-width: 1100px;
  width: 100%;
  text-align: center;
  position: relative;
  z-index: 2;
}
.hero-eyebrow {
  display: inline-block;
  font-weight: 800;
  font-size: 0.85rem;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--accent);
  padding: 0.5rem 1.2rem;
  border: 2px solid rgba(245, 158, 11, 0.4);
  border-radius: var(--radius-btn);
  margin-bottom: 1.8rem;
}
.hero h1 {
  font-size: clamp(3rem, 8vw, 6rem);
  font-weight: 900;
  letter-spacing: -0.04em;
  margin-bottom: 1.5rem;
}
.hero h1 span {
  color: var(--accent);
  position: relative;
  display: inline-block;
}
.hero-sub {
  font-size: clamp(1.05rem, 1.6vw, 1.3rem);
  font-weight: 400;
  max-width: 650px;
  margin: 0 auto 2.5rem;
  opacity: 0.92;
}
.hero-btns {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
}

/* BUTTONS */
.btn-primary, .btn-secondary, .btn-outline {
  display: inline-block;
  padding: 1rem 2.2rem;
  border-radius: var(--radius-btn);
  font-family: var(--font-heading);
  font-weight: 800;
  font-size: 1rem;
  cursor: pointer;
  border: none;
  transition: all 0.3s ease;
  letter-spacing: -0.01em;
}
.btn-primary {
  background: var(--gradient);
  color: #fff;
  box-shadow: 0 8px 24px rgba(124, 58, 237, 0.45), 0 0 0 0 rgba(245, 158, 11, 0.6);
  animation: pulse-glow 2.4s infinite;
}
.btn-primary:hover {
  background: linear-gradient(135deg, #8b5cf6, #a855f7);
  transform: scale(1.04) translateY(-2px);
  box-shadow: 0 14px 32px rgba(124, 58, 237, 0.6);
}
.btn-secondary {
  background: var(--accent);
  color: #111;
  box-shadow: 0 8px 24px rgba(245, 158, 11, 0.45);
}
.btn-secondary:hover {
  background: #fbbf24;
  transform: scale(1.04) translateY(-2px);
  box-shadow: 0 14px 32px rgba(245, 158, 11, 0.6);
}
.btn-outline {
  background: transparent;
  color: #fff;
  border: 2px solid rgba(255, 255, 255, 0.6);
}
.btn-outline:hover {
  background: #fff;
  color: var(--primary);
  transform: scale(1.04);
}

@keyframes pulse-glow {
  0%, 100% { box-shadow: 0 8px 24px rgba(124, 58, 237, 0.45), 0 0 0 0 rgba(245, 158, 11, 0.5); }
  50% { box-shadow: 0 8px 24px rgba(124, 58, 237, 0.45), 0 0 0 14px rgba(245, 158, 11, 0); }
}

/* SECTION HEADING */
section { padding: 6rem 5vw; }
.section-heading {
  text-align: center;
  margin-bottom: 3.5rem;
}
.section-heading h2 {
  font-size: clamp(2.2rem, 4.5vw, 3.8rem);
  margin-bottom: 1rem;
}
.section-heading h2 span { color: var(--primary); }
.section-sub {
  font-size: 1.1rem;
  color: var(--text-soft);
  max-width: 620px;
  margin: 0 auto;
}

/* ABOUT */
.about { background: var(--surface); }
.about-inner {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: center;
}
.about-text h2 {
  font-size: clamp(2rem, 4vw, 3.2rem);
  margin-bottom: 1.5rem;
}
.about-text p {
  color: var(--text-soft);
  margin-bottom: 1.2rem;
  font-size: 1.05rem;
}
.about-highlight {
  display: inline-block;
  margin-top: 1rem;
  padding: 1rem 1.5rem;
  background: linear-gradient(135deg, rgba(124, 58, 237, 0.08), rgba(245, 158, 11, 0.08));
  border-left: 4px solid var(--accent);
  border-radius: 0 12px 12px 0;
  font-weight: 800;
  color: var(--primary);
}
.about-img {
  border-radius: var(--radius-card);
  overflow: hidden;
  box-shadow: var(--shadow);
  aspect-ratio: 4/5;
  background: var(--gradient);
}
.about-img img { width: 100%; height: 100%; object-fit: cover; }

/* CARDS GRID (SERVICES) */
.cards-grid {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}
.card {
  background: var(--surface);
  border-radius: var(--radius-card);
  border-left: 4px solid var(--primary);
  padding: 2.2rem 1.8rem;
  box-shadow: var(--shadow);
  transition: transform 0.35s ease, box-shadow 0.35s ease;
  position: relative;
}
.card:hover {
  transform: translateY(-6px) scale(1.03);
  box-shadow: var(--shadow-hover);
}
.card-icon {
  width: 56px; height: 56px;
  border-radius: 14px;
  background: var(--gradient);
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 1.6rem;
  margin-bottom: 1.3rem;
  box-shadow: 0 8px 20px rgba(124, 58, 237, 0.35);
}
.card-title {
  font-size: 1.4rem;
  font-weight: 900;
  margin-bottom: 0.7rem;
}
.card-desc {
  color: var(--text-soft);
  font-size: 0.98rem;
  margin-bottom: 1.3rem;
}
.card-price {
  font-family: var(--font-heading);
  font-weight: 900;
  font-size: 1.8rem;
  color: var(--accent);
  letter-spacing: -0.03em;
}

/* FEATURES */
.feats-grid {
  max-width: 1100px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 2rem;
}
.feat {
  display: flex;
  gap: 1.3rem;
  padding: 2rem;
  background: var(--surface);
  border-radius: var(--radius-card);
  border-left: 4px solid var(--primary);
  box-shadow: var(--shadow);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.feat:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
}
.feat-icon {
  flex-shrink: 0;
  width: 54px; height: 54px;
  border-radius: 14px;
  background: linear-gradient(135deg, var(--accent), #fbbf24);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5rem;
  color: #fff;
}
.feat-title {
  font-size: 1.2rem;
  font-weight: 900;
  margin-bottom: 0.4rem;
}
.feat-text {
  color: var(--text-soft);
  font-size: 0.95rem;
}

/* REVIEWS */
.reviews-grid {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}
.review-card {
  background: var(--surface);
  border-radius: var(--radius-card);
  padding: 2rem;
  box-shadow: var(--shadow);
  border-left: 4px solid var(--accent);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.review-card:hover {
  transform: translateY(-6px) scale(1.03);
  box-shadow: var(--shadow-hover);
}
.review-stars {
  color: var(--accent);
  font-size: 1.1rem;
  margin-bottom: 1rem;
  letter-spacing: 2px;
}
.review-text {
  color: var(--text-soft);
  font-style: italic;
  margin-bottom: 1.3rem;
  font-size: 1rem;
}
.review-author {
  font-weight: 800;
  color: var(--primary);
}

/* FAQ */
.faq-list {
  max-width: 820px;
  margin: 0 auto;
}
.faq-item {
  background: var(--surface);
  border-radius: 16px;
  margin-bottom: 1rem;
  border-left: 4px solid var(--primary);
  box-shadow: 0 4px 14px rgba(124, 58, 237, 0.08);
  overflow: hidden;
  transition: box-shadow 0.3s;
}
.faq-item[open] {
  box-shadow: var(--shadow);
}
.faq-q {
  padding: 1.3rem 1.6rem;
  font-weight: 800;
  font-size: 1.05rem;
  cursor: pointer;
  list-style: none;
  display: flex;
  justify-content: space-between;
  align-items: center;
  color: var(--text);
}
.faq-q::-webkit-details-marker { display: none; }
.faq-q::after {
  content: '+';
  font-size: 1.6rem;
  color: var(--primary);
  font-weight: 900;
  transition: transform 0.3s;
}
.faq-item[open] .faq-q::after {
  transform: rotate(45deg);
}
.faq-a {
  padding: 0 1.6rem 1.4rem;
  color: var(--text-soft);
  line-height: 1.7;
}

/* CTA BAND */
.cta-band {
  background: var(--gradient);
  color: #fff;
  text-align: center;
  padding: 6rem 5vw;
  position: relative;
  overflow: hidden;
}
.cta-band::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(circle at 20% 50%, rgba(245, 158, 11, 0.2), transparent 60%);
}
.cta-band h2 {
  font-size: clamp(2.2rem, 5vw, 4rem);
  margin-bottom: 1rem;
  position: relative;
}
.cta-band p {
  font-size: 1.15rem;
  margin-bottom: 2.2rem;
  opacity: 0.95;
  position: relative;
}
.cta-band .btn-cream {
  display: inline-block;
  background: var(--accent);
  color: #111;
  padding: 1.1rem 2.4rem;
  border-radius: var(--radius-btn);
  font-weight: 900;
  font-size: 1.05rem;
  box-shadow: 0 10px 30px rgba(245, 158, 11, 0.5);
  transition: all 0.3s;
  position: relative;
}
.cta-band .btn-cream:hover {
  background: #fbbf24;
  transform: scale(1.05) translateY(-2px);
  box-shadow: 0 14px 40px rgba(245, 158, 11, 0.7);
}

/* CONTACT */
.contact-grid {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
}
.contact-info {
  background: var(--surface);
  border-radius: var(--radius-card);
  padding: 2.5rem;
  box-shadow: var(--shadow);
  border-left: 4px solid var(--accent);
}
.contact-info h3 {
  font-size: 1.6rem;
  margin-bottom: 1.3rem;
  color: var(--primary);
}
.contact-info p {
  color: var(--text-soft);
  margin-bottom: 0.8rem;
}
.contact-form {
  background: var(--surface);
  border-radius: var(--radius-card);
  padding: 2.5rem;
  box-shadow: var(--shadow);
}
.form-group {
  margin-bottom: 1.3rem;
}
.form-group label {
  display: block;
  font-weight: 800;
  margin-bottom: 0.5rem;
  font-size: 0.9rem;
  color: var(--text);
}
.form-group input,
.form-group textarea {
  width: 100%;
  padding: 0.9rem 1.1rem;
  border: 2px solid #e5e7eb;
  border-radius: 12px;
  font-family: var(--font-body);
  font-size: 1rem;
  background: #fafafa;
  transition: border-color 0.3s, box-shadow 0.3s, background 0.3s;
}
.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--primary);
  background: #fff;
  box-shadow: 0 0 0 4px rgba(124, 58, 237, 0.15);
}
.form-group textarea {
  min-height: 130px;
  resize: vertical;
}
.form-submit {
  width: 100%;
  background: var(--gradient);
  color: #fff;
  border: none;
  padding: 1.1rem;
  border-radius: var(--radius-btn);
  font-family: var(--font-heading);
  font-weight: 900;
  font-size: 1.05rem;
  cursor: pointer;
  box-shadow: 0 8px 24px rgba(124, 58, 237, 0.4);
  transition: all 0.3s;
}
.form-submit:hover {
  transform: scale(1.02) translateY(-2px);
  box-shadow: 0 14px 32px rgba(124, 58, 237, 0.6);
}
.form-submit:active { transform: scale(0.99); }

.map-embed {
  width: 100%;
  height: 320px;
  border-radius: var(--radius-card);
  border: none;
  margin-top: 2rem;
  box-shadow: var(--shadow);
}

/* FOOTER */
footer {
  background: #0a0a0a;
  color: #e5e7eb;
  padding: 4rem 5vw 1.5rem;
}
.footer-inner {
  max-width: 1200px;
  margin: 0 auto;
}
.footer-top {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr;
  gap: 3rem;
  padding-bottom: 2.5rem;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}
.footer-brand p {
  color: #9ca3af;
  margin-top: 0.8rem;
  max-width: 380px;
}
.footer-name {
  font-family: var(--font-heading);
  font-weight: 900;
  font-size: 1.6rem;
  letter-spacing: -0.03em;
  background: var(--gradient);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}
.footer-links {
  display: flex;
  flex-direction: column;
  gap: 0.7rem;
  list-style: none;
}
.footer-links a {
  color: #d1d5db;
  font-weight: 400;
  transition: color 0.25s;
}
.footer-links a:hover { color: var(--accent); }
.`,
  },

  lujo: {
    name: 'Lujo',
    description: 'Premium, sofisticado, exclusivo',
    accentColor: '#c9a96e',
    bgColor: '#0f0f0f',
    textColor: '#f5f0e8',
    promptDirective: `Generate a complete, single-page HTML document for a high-end Spanish luxury business (restaurante de alta cocina, hotel boutique, spa premium, champagne bar, or club privado). You MUST use the EXACT standardised class names provided: .nav, .nav-logo, .nav-links, .nav-cta, .hero, .hero-inner, .hero-eyebrow, .hero-sub, .hero-btns, .btn-primary, .btn-secondary, .btn-outline, .section-heading, .section-sub, .about, .about-inner, .about-text, .about-img, .about-highlight, .cards-grid, .card, .card-icon, .card-title, .card-desc, .card-price, .feats-grid, .feat, .feat-icon, .feat-title, .feat-text, .reviews-grid, .review-card, .review-stars, .review-text, .review-author, .faq-list, .faq-item, .faq-q, .faq-a, .cta-band, .contact-grid, .contact-info, .contact-form, .form-group, .form-submit, .footer-inner, .footer-top, .footer-brand, .footer-name, .footer-links, .footer-bottom, .fade-in, .map-embed. Include this Google Font link in the <head>: <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">. Respect the exact palette: background #0f0f0f, surface #1a1a1a, gold #c9a96e, cream text #f5f0e8, muted rgba(245,240,232,.6). The tone is whispered exclusivity — understated, opulent, never loud. Use Cormorant Garamond 300 for large headings in cream, Inter 300 for body copy. All sections must be dark, alternating between #0f0f0f and #1a1a1a, separated by thin gold hairlines or diamond/star gold icons. Hero must be a full-bleed dark-overlay image. Buttons are outlined gold, uppercase, wide letter-spacing. Write all copy in refined Spanish (Castellano) adapted to the business type. Include hero, about, services (3-6), features, reviews, FAQ, CTA band, contact with form and map, and footer.`,
    cssInject: `@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600&family=Inter:wght@300;400;500&display=swap');

:root{
  --primary:#c9a96e;
  --accent:#c9a96e;
  --bg:#0f0f0f;
  --surface:#1a1a1a;
  --text:#f5f0e8;
  --muted:rgba(245,240,232,.6);
  --border:rgba(201,169,110,.25);
  --border-strong:rgba(201,169,110,.55);
  --radius-card:8px;
  --radius-btn:4px;
  --shadow:0 0 30px rgba(201,169,110,.06);
  --shadow-hover:0 0 50px rgba(201,169,110,.14);
  --font-heading:'Cormorant Garamond', serif;
  --font-body:'Inter', sans-serif;
}

*{box-sizing:border-box;margin:0;padding:0}

html{scroll-behavior:smooth}

body{
  font-family:var(--font-body);
  font-weight:300;
  background:var(--bg);
  color:var(--text);
  line-height:1.85;
  font-size:16px;
  overflow-x:hidden;
  position:relative;
}

body::before{
  content:"";
  position:fixed;
  inset:0;
  pointer-events:none;
  z-index:1;
  opacity:.035;
  background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='200' height='200'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='2' stitchTiles='stitch'/></filter><rect width='100%25' height='100%25' filter='url(%23n)'/></svg>");
}

h1,h2,h3,h4{
  font-family:var(--font-heading);
  font-weight:300;
  letter-spacing:.01em;
  line-height:1.15;
  color:var(--text);
}

p{color:var(--muted)}

a{color:var(--primary);text-decoration:none;transition:color .3s ease}
a:hover{color:var(--text)}

section{
  padding:120px 32px;
  position:relative;
  z-index:2;
}

section:nth-of-type(even){background:var(--surface)}

/* NAV */
.nav{
  position:fixed;
  top:0;left:0;right:0;
  z-index:100;
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:24px 48px;
  background:transparent;
  transition:all .4s ease;
  border-bottom:1px solid transparent;
}
.nav.scrolled{
  background:rgba(15,15,15,.92);
  backdrop-filter:blur(12px);
  padding:16px 48px;
  border-bottom:1px solid var(--border);
}
.nav-logo{
  font-family:var(--font-heading);
  font-size:1.6rem;
  font-weight:400;
  color:var(--text);
  letter-spacing:.08em;
}
.nav-logo span{color:var(--primary)}
.nav-links{
  display:flex;
  gap:40px;
  list-style:none;
}
.nav-links li a{
  color:var(--text);
  font-size:.78rem;
  letter-spacing:.18em;
  text-transform:uppercase;
  font-weight:300;
  position:relative;
  padding:6px 0;
}
.nav-links li a::after{
  content:"";
  position:absolute;
  bottom:0;left:50%;
  width:0;height:1px;
  background:var(--primary);
  transition:all .4s ease;
  transform:translateX(-50%);
}
.nav-links li a:hover{color:var(--primary)}
.nav-links li a:hover::after{width:100%}

.nav-cta{
  border:1px solid var(--primary);
  color:var(--primary);
  background:transparent;
  padding:10px 24px;
  border-radius:var(--radius-btn);
  font-size:.7rem;
  letter-spacing:.14em;
  text-transform:uppercase;
  font-weight:400;
  cursor:pointer;
  transition:all .4s ease;
}
.nav-cta:hover{background:var(--primary);color:var(--bg)}

.nav-hamburger{
  display:none;
  flex-direction:column;
  gap:5px;
  background:none;
  border:none;
  cursor:pointer;
}
.nav-hamburger span{
  width:26px;height:1px;background:var(--primary);transition:.3s
}

/* HERO */
.hero{
  min-height:100vh;
  display:flex;
  align-items:center;
  justify-content:center;
  text-align:center;
  padding:120px 32px 80px;
  position:relative;
  background:linear-gradient(rgba(0,0,0,.72),rgba(0,0,0,.72)),url('https://images.unsplash.com/photo-1414235077428-338989a2e8c0?w=1920') center/cover no-repeat;
  overflow:hidden;
}
.hero::after{
  content:"";
  position:absolute;
  left:50%;bottom:40px;
  width:1px;height:60px;
  background:linear-gradient(to bottom,transparent,var(--primary));
  transform:translateX(-50%);
  animation:drop 2.4s ease-in-out infinite;
}
@keyframes drop{
  0%,100%{opacity:.3;transform:translate(-50%,0)}
  50%{opacity:1;transform:translate(-50%,8px)}
}
.hero-inner{max-width:900px;position:relative;z-index:2}
.hero-eyebrow{
  color:var(--primary);
  font-family:var(--font-body);
  font-size:.75rem;
  letter-spacing:.42em;
  text-transform:uppercase;
  margin-bottom:32px;
  font-weight:300;
}
.hero-eyebrow::before,.hero-eyebrow::after{
  content:"◆";
  margin:0 18px;
  font-size:.5rem;
  vertical-align:middle;
  opacity:.7;
}
.hero h1{
  font-size:clamp(2.8rem,6vw,5.4rem);
  font-weight:300;
  margin-bottom:32px;
  color:var(--text);
  letter-spacing:.02em;
}
.hero-sub{
  font-size:1.05rem;
  max-width:620px;
  margin:0 auto 48px;
  color:var(--muted);
  font-weight:300;
}
.hero-btns{
  display:flex;
  gap:20px;
  justify-content:center;
  flex-wrap:wrap;
}

/* BUTTONS */
.btn-primary,.btn-secondary,.btn-outline,.btn-cream,.form-submit{
  display:inline-block;
  padding:16px 38px;
  font-family:var(--font-body);
  font-size:.72rem;
  font-weight:400;
  letter-spacing:.22em;
  text-transform:uppercase;
  border-radius:var(--radius-btn);
  cursor:pointer;
  transition:all .45s ease;
  border:1px solid var(--primary);
  background:transparent;
  color:var(--primary);
}
.btn-primary{
  animation:pulseGold 3.5s ease-in-out infinite;
}
@keyframes pulseGold{
  0%,100%{box-shadow:0 0 0 0 rgba(201,169,110,.25)}
  50%{box-shadow:0 0 0 14px rgba(201,169,110,0)}
}
.btn-primary:hover,.btn-secondary:hover,.btn-outline:hover,.form-submit:hover{
  background:var(--primary);
  color:var(--bg);
  letter-spacing:.26em;
}
.btn-secondary{
  border-color:var(--muted);
  color:var(--text);
}
.btn-secondary:hover{background:var(--text);color:var(--bg);border-color:var(--text)}

.btn-cream{
  border:1px solid var(--text);
  color:var(--text);
}
.btn-cream:hover{background:var(--text);color:var(--bg)}

/* SECTION HEADINGS */
.section-heading{
  text-align:center;
  margin-bottom:80px;
}
.section-heading h2{
  font-size:clamp(2.2rem,4vw,3.6rem);
  font-weight:300;
  margin-bottom:20px;
}
.section-heading::before{
  content:"";
  display:block;
  width:60px;height:1px;
  background:var(--primary);
  margin:0 auto 28px;
}
.section-sub{
  color:var(--muted);
  max-width:620px;
  margin:0 auto;
  font-size:1rem;
}

/* ABOUT */
.about-inner{
  max-width:1200px;
  margin:0 auto;
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:80px;
  align-items:center;
}
.about-text h2{
  font-size:clamp(2rem,3.6vw,3.2rem);
  margin-bottom:28px;
}
.about-text p{margin-bottom:20px}
.about-img{
  width:100%;
  aspect-ratio:4/5;
  overflow:hidden;
  border-radius:var(--radius-card);
  border:1px solid var(--border);
}
.about-img img{
  width:100%;height:100%;
  object-fit:cover;display:block;
  filter:grayscale(.15) brightness(.9);
}
.about-highlight{
  display:inline-block;
  margin-top:28px;
  padding:18px 0;
  border-top:1px solid var(--border);
  border-bottom:1px solid var(--border);
  color:var(--primary);
  font-family:var(--font-heading);
  font-style:italic;
  font-size:1.25rem;
  letter-spacing:.04em;
}

/* CARDS */
.cards-grid{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:32px;
  max-width:1200px;
  margin:0 auto;
}
.card{
  background:var(--surface);
  border:1px solid var(--border);
  border-radius:var(--radius-card);
  padding:48px 36px;
  box-shadow:var(--shadow);
  transition:all .5s ease;
  text-align:center;
  position:relative;
  overflow:hidden;
}
.card::before{
  content:"";
  position:absolute;
  top:0;left:50%;
  width:0;height:1px;
  background:var(--primary);
  transition:all .5s ease;
  transform:translateX(-50%);
}
.card:hover{
  transform:translateY(-4px);
  border-color:var(--border-strong);
  box-shadow:var(--shadow-hover);
}
.card:hover::before{width:60%}
.card-icon{
  font-size:1.8rem;
  color:var(--primary);
  margin-bottom:24px;
  display:inline-block;
}
.card-title{
  font-size:1.55rem;
  margin-bottom:16px;
  color:var(--text);
  font-weight:400;
}
.card-desc{
  color:var(--muted);
  margin-bottom:24px;
  font-size:.95rem;
}
.card-price{
  color:var(--primary);
  font-family:var(--font-heading);
  font-size:1.6rem;
  font-weight:400;
  padding-top:20px;
  border-top:1px solid var(--border);
  letter-spacing:.04em;
}

/* FEATURES */
.feats-grid{
  display:grid;
  grid-template-columns:repeat(2,1fr);
  gap:48px;
  max-width:1100px;
  margin:0 auto;
}
.feat{
  display:flex;
  gap:28px;
  padding:32px;
  border:1px solid var(--border);
  border-radius:var(--radius-card);
  background:rgba(26,26,26,.5);
  transition:all .4s ease;
}
.feat:hover{
  border-color:var(--border-strong);
  transform:translateY(-2px);
}
.feat-icon{
  font-size:1.6rem;
  color:var(--primary);
  flex-shrink:0;
}
.feat-title{
  font-size:1.35rem;
  margin-bottom:10px;
  font-weight:400;
}
.feat-text{color:var(--muted);font-size:.95rem}

/* REVIEWS */
.reviews-grid{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:32px;
  max-width:1200px;
  margin:0 auto;
}
.review-card{
  background:var(--surface);
  border:1px solid var(--border);
  border-radius:var(--radius-card);
  padding:42px 34px;
  box-shadow:var(--shadow);
  transition:all .5s ease;
  position:relative;
}
.review-card::before{
  content:"“";
  position:absolute;
  top:8px;right:28px;
  font-family:var(--font-heading);
  font-size:5rem;
  color:var(--primary);
  opacity:.25;
  line-height:1;
}
.review-card:hover{
  transform:translateY(-4px);
  border-color:var(--border-strong);
  box-shadow:var(--shadow-hover);
}
.review-stars{
  color:var(--primary);
  letter-spacing:.25em;
  margin-bottom:20px;
  font-size:.85rem;
}
.review-text{
  color:var(--text);
  font-family:var(--font-heading);
  font-style:italic;
  font-size:1.1rem;
  line-height:1.7;
  font-weight:300;
  margin-bottom:24px;
}
.review-author{
  color:var(--primary);
  font-size:.75rem;
  letter-spacing:.22em;
  text-transform:uppercase;
  padding-top:18px;
  border-top:1px solid var(--border);
}

/* FAQ */
.faq-list{
  max-width:820px;
  margin:0 auto;
}
.faq-item{
  border-bottom:1px solid var(--border);
  padding:28px 0;
  transition:all .3s ease;
}
.faq-q{
  cursor:pointer;
  font-family:var(--font-heading);
  font-size:1.35rem;
  font-weight:400;
  color:var(--text);
  list-style:none;
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:24px;
  transition:color .3s ease;
}
.faq-q::-webkit-details-marker{display:none}
.faq-q::after{
  content:"+";
  color:var(--primary);
  font-size:1.5rem;
  font-weight:300;
  transition:transform .4s ease;
}
.faq-item[open] .faq-q{color:var(--primary)}
.faq-item[open] .faq-q::after{transform:rotate(45deg)}
.faq-a{
  color:var(--muted);
  margin-top:18px;
  font-size:.98rem;
  padding-right:40px;
}

/* CTA BAND */
.cta-band{
  text-align:center;
  padding:140px 32px;
  background:linear-gradient(rgba(15,15,15,.88),rgba(15,15,15,.88)),url('https://images.unsplash.com/photo-1551218808-94e220e084d2?w=1920') center/cover fixed;
  border-top:1px solid var(--border);
  border-bottom:1px solid var(--border);
}
.cta-band h2{
  font-size:clamp(2.2rem,4.5vw,3.8rem);
  margin-bottom:24px;
  font-weight:300;
}
.cta-band p{
  max-width:560px;
  margin:0 auto 40px;
  color:var(--muted);
  font-size:1.05rem;
}

/* CONTACT */
.contact-grid{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:64px;
  max-width:1200px;
  margin:0 auto;
}
.contact-info p{margin-bottom:18px;color:var(--muted)}
.contact-info strong{color:var(--primary);font-weight:400;letter-spacing:.1em;text-transform:uppercase;font-size:.78rem;display:block;margin-bottom:4px}
.contact-form{
  background:var(--surface);
  padding:48px;
  border-radius:var(--radius-card);
  border:1px solid var(--border);
}
.form-group{margin-bottom:24px}
.form-group label{
  display:block;
  font-size:.72rem;
  letter-spacing:.2em;
  text-transform:uppercase;
  color:var(--primary);
  margin-bottom:10px;
  font-weight:400;
}
.form-group input,.form-group textarea{
  width:100%;
  background:transparent;
  border:none;
  border-bottom:1px solid var(--border);
  padding:12px 2px;
  color:var(--text);
  font-family:var(--font-body);
  font-size:.95rem;
  font-weight:300;
  transition:all .4s ease;
}
.form-group input:focus,.form-group textarea:focus{
  outline:none;
  border-bottom-color:var(--primary);
}
.form-group textarea{min-height:120px;resize:vertical}
.form-submit{width:100%;margin-top:12px}

.map-embed{
  width:100%;
  height:360px;
  border:1px solid var(--border);
  border-radius:var(--radius-card);
  filter:grayscale(1) brightness(.6) invert(.88);
  margin-top:48px;
}

/* FOOTER */
footer{
  background:#0a0a0a;
  padding:80px 32px 32px;
  border-top:1px solid var(--border);
  position:relative;
  z-index:2;
}
.footer-inner{max-width:1200px;margin:0 auto}
.footer-top{
  display:grid;
  grid-template-columns:2fr 1fr 1fr 1fr;
  gap:48px;
  padding-bottom:56px;
  border-bottom:1px solid var(--border);
}
.footer-brand p{margin-top:16px;font-size:.92rem}
.footer-name{
  font-family:var(--font-heading);
  font-size:1.7rem;
  color:var(--text);
  letter-spacing:.08em;
  font-weight:400;
}
.footer-name span{color:var(--primary)}
.footer-links{list-style:none;display:flex;flex-direction:column;gap:12px}
.footer-links a{
  color:var(--muted);
  font-size:.88rem;
  letter-spacing:.08em;
  transition:color .3s ease;
}
.footer-links a:hover{color:var(--primary)}
.footer-bottom{
  padding-top:32px;
  text-align:center;
  color:var(--muted);
  font-size:.78rem;
  letter-spacing:.18em;
  text-transform:uppercase;
}

/* UTILS */
.fade-in{opacity:0;transform:translateY(20px);transition:opacity .6s ease,transform .6s ease}
.fade-in.visible{opacity:1;transform:none}

/* RESPONSIVE */
@media (max-width:1024px){
  .cards-grid{grid-template-columns:repeat(2,1fr)}
  .reviews-grid{grid-template-columns:repeat(2,1fr)}
  .nav{padding:20px 32px}
  .footer-top{grid-template-columns:1fr 1fr}
  section{padding:100px 28px}
}

@media (max-width:768px){
  .nav{padding:18px 22px}
  .nav.scrolled{padding:14px 22px}
  .nav-links{
    display:none;
    position:absolute;
    top:100%;left:0;right:0;
    flex-direction:column;
    background:rgba(15,15,15,.98);
    padding:32px;
    gap:22px;
    border-top:1px solid var(--border);
  }
  .nav-links.open{display:flex}
  .nav-hamburger{display:flex}
  .nav-cta{display:none}

  section{padding:80px 22px}
  .hero{padding:100px 22px 60px}
  .hero-btns{flex-direction:column;align-items:stretch}
  .hero-btns .btn-primary,.hero-btns .btn-secondary{width:100%}

  .about-inner{grid-template-columns:1fr;gap:48px}
  .cards-grid,.feats-grid,.reviews-grid,.contact-grid{grid-template-columns:1fr;gap:24px}
  .feat{flex-direction:column;gap:16px}
  .contact-form{padding:32px 24px}
  .footer-top{grid-template-columns:1fr;gap:32px;text-align`,
  },

  fresco: {
    name: 'Fresco',
    description: 'Confianza, salud, bienestar',
    accentColor: '#0d7a5f',
    bgColor: '#f0f9f6',
    textColor: '#0f2922',
    promptDirective: `Generate a complete, single-page Spanish wellness/clinical website using the "Fresco" design system. You MUST use these EXACT class names from the standardised vocabulary — no variations, no custom class names: .nav, .nav-logo, .nav-links, .nav-cta, .nav-hamburger, .hero, .hero-inner, .hero-eyebrow, .hero-sub, .hero-btns, .btn-primary, .btn-secondary, .btn-outline, .section-heading, .section-sub, .about, .about-inner, .about-text, .about-img, .about-highlight, .cards-grid, .card, .card-icon, .card-title, .card-desc, .card-price, .feats-grid, .feat, .feat-icon, .feat-title, .feat-text, .reviews-grid, .review-card, .review-stars, .review-text, .review-author, .faq-list, .faq-item, .faq-q, .faq-a, .cta-band, .contact-grid, .contact-info, .contact-form, .form-group, .form-submit, .footer-inner, .footer-top, .footer-brand, .footer-name, .footer-links, .footer-bottom, .fade-in, .map-embed. Include in the <head>: <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700&display=swap" rel="stylesheet">. Follow the exact palette: forest green primary #0d7a5f, teal accent #38b2a0, mint background #f0f9f6, deep green text #0f2922, surface white #ffffff. The tone is clinical cleanliness meets natural warmth — trusted, calm, reassuring. Build a light hero with subtle mint-green overlay and a white pill CTA with green border. Use alternating white and mint sections separated by a thin teal gradient divider. Cards are clean white with soft green-tinted shadow and a teal circular icon. Use green checkmark bullets for feature lists and style trust badges prominently. Adapt all copy naturally in Spanish for the specific health business type (clinic, dentist, pharmacy, nutritionist, psychologist, physiotherapist), including realistic services, prices in €, certifications, and patient-focused reassurance language.`,
    cssInject: `@import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700&display=swap');

:root {
  --primary: #0d7a5f;
  --primary-dark: #095c47;
  --accent: #38b2a0;
  --accent-light: #5cc9b8;
  --bg: #f0f9f6;
  --text: #0f2922;
  --text-soft: #456055;
  --surface: #ffffff;
  --border: #d1fae5;
  --radius-card: 12px;
  --radius-btn: 10px;
  --shadow: 0 2px 12px rgba(13, 122, 95, 0.08);
  --shadow-hover: 0 12px 32px rgba(13, 122, 95, 0.15);
  --font-heading: 'Plus Jakarta Sans', sans-serif;
  --font-body: 'Plus Jakarta Sans', sans-serif;
  --divider: linear-gradient(90deg, transparent, var(--accent), transparent);
}

* { box-sizing: border-box; margin: 0; padding: 0; }

html { scroll-behavior: smooth; }

body {
  font-family: var(--font-body);
  font-weight: 400;
  line-height: 1.7;
  color: var(--text);
  background: var(--surface);
  -webkit-font-smoothing: antialiased;
}

h1, h2, h3, h4 {
  font-family: var(--font-heading);
  font-weight: 700;
  line-height: 1.2;
  color: var(--text);
}

img { max-width: 100%; display: block; }
a { color: inherit; text-decoration: none; }

/* ============ NAVIGATION ============ */
.nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 6%;
  background: transparent;
  z-index: 1000;
  transition: all 0.3s ease;
}

.nav.scrolled {
  background: rgba(255, 255, 255, 0.97);
  backdrop-filter: blur(12px);
  box-shadow: 0 2px 16px rgba(13, 122, 95, 0.07);
  padding: 12px 6%;
}

.nav-logo {
  font-family: var(--font-heading);
  font-weight: 700;
  font-size: 1.4rem;
  color: var(--primary);
  letter-spacing: -0.02em;
}

.nav-links {
  display: flex;
  gap: 36px;
  list-style: none;
}

.nav-links li a {
  font-size: 0.95rem;
  font-weight: 600;
  color: var(--text);
  transition: color 0.2s ease;
  position: relative;
}

.nav-links li a:hover { color: var(--primary); }

.nav-links li a::after {
  content: '';
  position: absolute;
  left: 0; bottom: -6px;
  width: 0; height: 2px;
  background: var(--accent);
  transition: width 0.25s ease;
}

.nav-links li a:hover::after { width: 100%; }

.nav-cta {
  background: var(--primary);
  color: #fff;
  padding: 10px 22px;
  border-radius: var(--radius-btn);
  font-weight: 600;
  font-size: 0.92rem;
  transition: all 0.25s ease;
}

.nav-cta:hover {
  background: var(--primary-dark);
  transform: translateY(-1px);
  box-shadow: 0 6px 18px rgba(13, 122, 95, 0.25);
}

.nav-hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  cursor: pointer;
  background: none;
  border: none;
  padding: 8px;
}

.nav-hamburger span {
  width: 24px;
  height: 2px;
  background: var(--text);
  border-radius: 2px;
  transition: all 0.3s ease;
}

/* ============ HERO ============ */
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  padding: 120px 6% 80px;
  background: linear-gradient(rgba(13, 122, 95, 0.25), rgba(13, 122, 95, 0.25)),
              linear-gradient(135deg, #e8f5f0 0%, #f0f9f6 100%);
  background-size: cover;
  background-position: center;
  overflow: hidden;
}

.hero::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(circle at 80% 20%, rgba(56, 178, 160, 0.15), transparent 50%);
  pointer-events: none;
}

.hero-inner {
  max-width: 860px;
  text-align: center;
  position: relative;
  z-index: 2;
}

.hero-eyebrow {
  display: inline-block;
  background: rgba(255, 255, 255, 0.85);
  color: var(--primary);
  padding: 8px 18px;
  border-radius: 999px;
  font-size: 0.85rem;
  font-weight: 600;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  margin-bottom: 24px;
  border: 1px solid var(--border);
}

.hero h1 {
  font-size: clamp(2.2rem, 5.5vw, 3.8rem);
  color: var(--primary-dark);
  letter-spacing: -0.025em;
  margin-bottom: 22px;
}

.hero-sub {
  font-size: 1.15rem;
  color: var(--text-soft);
  max-width: 640px;
  margin: 0 auto 38px;
  line-height: 1.7;
}

.hero-btns {
  display: flex;
  gap: 16px;
  justify-content: center;
  flex-wrap: wrap;
}

.hero-btns .btn-primary {
  background: #fff;
  color: var(--primary);
  border: 2px solid var(--primary);
  padding: 14px 32px;
  border-radius: 999px;
  animation: softPulse 3s ease-in-out infinite;
}

.hero-btns .btn-primary:hover {
  background: var(--primary);
  color: #fff;
}

@keyframes softPulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(13, 122, 95, 0.35); }
  50% { box-shadow: 0 0 0 12px rgba(13, 122, 95, 0); }
}

/* ============ BUTTONS ============ */
.btn-primary, .btn-secondary, .btn-outline {
  display: inline-block;
  padding: 12px 28px;
  border-radius: var(--radius-btn);
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 0.98rem;
  cursor: pointer;
  transition: all 0.25s ease;
  border: 2px solid transparent;
  text-align: center;
}

.btn-primary {
  background: var(--primary);
  color: #fff;
  border-color: var(--primary);
}

.btn-primary:hover {
  background: var(--primary-dark);
  border-color: var(--primary-dark);
  transform: translateY(-2px);
  box-shadow: 0 10px 22px rgba(13, 122, 95, 0.22);
}

.btn-secondary {
  background: transparent;
  color: var(--accent);
  border-color: var(--accent);
}

.btn-secondary:hover {
  background: var(--accent);
  color: #fff;
  transform: translateY(-2px);
}

.btn-outline {
  background: transparent;
  color: var(--primary);
  border-color: var(--primary);
}

.btn-outline:hover {
  background: var(--primary);
  color: #fff;
}

/* ============ SECTION HEADINGS ============ */
.section-heading {
  text-align: center;
  max-width: 720px;
  margin: 0 auto 56px;
}

.section-heading h2 {
  font-size: clamp(1.8rem, 3.5vw, 2.6rem);
  color: var(--primary-dark);
  margin-bottom: 14px;
  letter-spacing: -0.02em;
}

.section-sub {
  font-size: 1.05rem;
  color: var(--text-soft);
}

/* section divider */
section + section::before {
  content: '';
  display: block;
  height: 3px;
  background: var(--divider);
  width: 100%;
  max-width: 100%;
}

section {
  padding: 90px 6%;
}

section:nth-of-type(even) {
  background: var(--bg);
}

/* ============ ABOUT ============ */
.about-inner {
  display: grid;
  grid-template-columns: 1.1fr 1fr;
  gap: 64px;
  align-items: center;
  max-width: 1180px;
  margin: 0 auto;
}

.about-text h2 {
  font-size: clamp(1.8rem, 3.2vw, 2.4rem);
  color: var(--primary-dark);
  margin-bottom: 20px;
}

.about-text p {
  color: var(--text-soft);
  margin-bottom: 16px;
  font-size: 1.02rem;
}

.about-highlight {
  display: inline-block;
  background: var(--bg);
  border-left: 3px solid var(--accent);
  padding: 14px 20px;
  margin-top: 20px;
  border-radius: 0 10px 10px 0;
  font-weight: 600;
  color: var(--primary-dark);
}

.about-img {
  border-radius: 16px;
  overflow: hidden;
  box-shadow: var(--shadow);
  aspect-ratio: 4/5;
  background: linear-gradient(135deg, #d1fae5, #a7e8d8);
}

.about-img img { width: 100%; height: 100%; object-fit: cover; }

/* ============ SERVICES / CARDS ============ */
.cards-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
  max-width: 1200px;
  margin: 0 auto;
}

.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 36px 28px;
  box-shadow: var(--shadow);
  transition: all 0.3s ease;
  position: relative;
}

.card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
  border-color: var(--accent-light);
}

.card-icon {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: var(--accent);
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5rem;
  margin-bottom: 22px;
  transition: transform 0.3s ease;
}

.card:hover .card-icon { transform: scale(1.08) rotate(-4deg); }

.card-title {
  font-size: 1.2rem;
  font-weight: 700;
  color: var(--primary-dark);
  margin-bottom: 10px;
}

.card-desc {
  color: var(--text-soft);
  font-size: 0.98rem;
  margin-bottom: 18px;
}

.card-price {
  display: inline-block;
  font-weight: 700;
  color: var(--primary);
  font-size: 1.1rem;
  padding-top: 14px;
  border-top: 1px dashed var(--border);
  width: 100%;
}

/* ============ FEATURES ============ */
.feats-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 32px;
  max-width: 1100px;
  margin: 0 auto;
}

.feat {
  display: flex;
  gap: 20px;
  padding: 24px;
  background: var(--surface);
  border-radius: var(--radius-card);
  border: 1px solid var(--border);
  transition: all 0.3s ease;
}

.feat:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow);
}

.feat-icon {
  flex-shrink: 0;
  width: 48px;
  height: 48px;
  border-radius: 50%;
  background: var(--bg);
  color: var(--primary);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.3rem;
}

.feat-icon::before {
  content: '✓';
  font-weight: 700;
  color: var(--primary);
}

.feat-title {
  font-size: 1.1rem;
  font-weight: 700;
  color: var(--primary-dark);
  margin-bottom: 6px;
}

.feat-text {
  color: var(--text-soft);
  font-size: 0.97rem;
}

/* ============ REVIEWS ============ */
.reviews-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
  max-width: 1200px;
  margin: 0 auto;
}

.review-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 30px;
  box-shadow: var(--shadow);
  transition: all 0.3s ease;
}

.review-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
}

.review-stars {
  color: #f6b93b;
  font-size: 1.1rem;
  margin-bottom: 14px;
  letter-spacing: 2px;
}

.review-text {
  color: var(--text-soft);
  font-style: italic;
  margin-bottom: 20px;
  font-size: 0.98rem;
}

.review-author {
  font-weight: 700;
  color: var(--primary-dark);
  font-size: 0.95rem;
  padding-top: 14px;
  border-top: 1px solid var(--border);
}

/* ============ FAQ ============ */
.faq-list {
  max-width: 820px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.faq-item {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 4px 24px;
  transition: all 0.25s ease;
  box-shadow: var(--shadow);
}

.faq-item[open] {
  border-color: var(--accent);
  box-shadow: 0 6px 20px rgba(56, 178, 160, 0.12);
}

.faq-q {
  cursor: pointer;
  font-weight: 700;
  color: var(--primary-dark);
  padding: 18px 0;
  list-style: none;
  position: relative;
  padding-right: 36px;
  font-size: 1.02rem;
}

.faq-q::-webkit-details-marker { display: none; }

.faq-q::after {
  content: '+';
  position: absolute;
  right: 0; top: 50%;
  transform: translateY(-50%);
  color: var(--accent);
  font-size: 1.6rem;
  font-weight: 400;
  transition: transform 0.3s ease;
}

.faq-item[open] .faq-q::after {
  transform: translateY(-50%) rotate(45deg);
}

.faq-a {
  padding: 0 0 20px;
  color: var(--text-soft);
  line-height: 1.7;
}

/* ============ CTA BAND ============ */
.cta-band {
  background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
  text-align: center;
  padding: 80px 6%;
  color: #fff;
  position: relative;
  overflow: hidden;
}

.cta-band::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(circle at 20% 50%, rgba(255,255,255,0.08), transparent 50%);
}

.cta-band h2 {
  color: #fff;
  font-size: clamp(1.8rem, 3.5vw, 2.6rem);
  margin-bottom: 16px;
  position: relative;
}

.cta-band p {
  font-size: 1.1rem;
  opacity: 0.92;
  margin-bottom: 32px;
  max-width: 620px;
  margin-left: auto;
  margin-right: auto;
  position: relative;
}

.cta-band .btn-cream {
  display: inline-block;
  background: #fff;
  color: var(--primary);
  padding: 14px 34px;
  border-radius: 999px;
  font-weight: 700;
  font-family: var(--font-heading);
  transition: all 0.25s ease;
  position: relative;
  border: 2px solid #fff;
}

.cta-band .btn-cream:hover {
  transform: translateY(-2px);
  box-shadow: 0 12px 26px rgba(0, 0, 0, 0.18);
  background: transparent;
  color: #fff;
}

/* ============ CONTACT ============ */
.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1.2fr;
  gap: 48px;
  max-width: 1180px;
  margin: 0 auto;
}

.contact-info {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 36px;
  box-shadow: var(--shadow);
}

.contact-info h3 {
  color: var(--primary-dark);
  margin-bottom: 18px;
  font-size: 1.3rem;
}

.contact-info p {
  color: var(--text-soft);
  margin-bottom: 12px;
  display: flex;
  align-items: center;
  gap: 10px;
}

.contact-form {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 36px;
  box-shadow: var(--shadow);
}

.form-group {
  margin-bottom: 20px;
}

.form-group label {
  display: block;
  font-weight: 600;
  color: var(--primary-dark);
  margin-bottom: 8px;
  font-size: 0.92rem;
}

.form-group input,
.form-group textarea {
  width: 100%;
  padding: 12px 16px;
  border: 1.5px solid var(--border);
  border-radius: var(--radius-btn);
  background: var(--bg);
  font-family: var(--font-body);
  font-size: 0.98rem;
  color: var(--text);
  transition: all 0.25s ease;
}

.form-group textarea {
  min-height: 130px;
  resize: vertical;
}

.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--accent);
  background: #fff;
  box-shadow: 0 0 0 4px rgba(56, 178, 160, 0.18);
}

.form-submit {
  width: 100%;
  background: var(--primary);
  color: #fff;
  border: none;
  padding: 14px;
  border-radius: var(--radius-btn);
  font-family: var(--font-heading);
  font-weight: 700;
  font-size: 1rem;
  cursor: pointer;
  transition: all 0.25s ease;
}

.form-submit:hover {
  background: var(--primary-dark);
  transform: translateY(-2px);
  box-shadow: 0 10px 22px rgba(13, 122, 95, 0.25);
}

.form-submit:active { transform: translateY(0); }

.map-embed {
  margin-top: 48px;
  max-width: 1180px;
  margin-left: auto;
  margin-right: auto;
  border-radius: var(--radius-card);
  overflow: hidden;
  border: 1px solid var(--border);
  box-shadow: var(--shadow);
}

.map-embed iframe {
  width: 100%;
  height: 380px;
  border: 0;
  display: block;
}

/* ============ FOOTER ============ */
footer {
  background: var(--text);
  color: #d1fae5;
  padding: 64px 6% 24px;
}

.footer-inner {
  max-width: 1200px;
  margin: 0 auto;
}

.footer-top {
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr 1fr;
  gap: 48px;
  padding-bottom: 40px;
  border-bottom: 1px solid rgba(209, 250, 229, 0.12);
}

.footer-brand p {
  margin-top: 14px;
  color: rgba(209, 250, 229, 0.7);
  font-size: 0.95rem;
}

.footer-name {
  font-family: var(--font-heading);
  font-weight: 700;
  font`,
  },

  urbano: {
    name: 'Urbano',
    description: 'Contemporáneo, ciudad, lifestyle',
    accentColor: '#0f4c81',
    bgColor: '#f5f7fa',
    textColor: '#1a2332',
    promptDirective: `Generate a complete, single-file HTML document for a modern Spanish local business website using the "Urbano" design system — a contemporary urban lifestyle aesthetic perfect for cafés, barbershops, co-working spaces, urban restaurants, and lifestyle retail. You MUST use the EXACT class names provided in the standardised vocabulary: .nav, .nav-logo, .nav-links, .nav-cta, .nav-hamburger, .hero, .hero-inner, .hero-eyebrow, .hero-sub, .hero-btns, .btn-primary, .btn-secondary, .btn-outline, .section-heading, .section-sub, .about, .about-inner, .about-text, .about-img, .about-highlight, .cards-grid, .card, .card-icon, .card-title, .card-desc, .card-price, .feats-grid, .feat, .feat-icon, .feat-title, .feat-text, .reviews-grid, .review-card, .review-stars, .review-text, .review-author, .faq-list, .faq-item, .faq-q, .faq-a, .cta-band, .btn-cream, .contact-grid, .contact-info, .contact-form, .form-group, .form-submit, .footer-inner, .footer-top, .footer-brand, .footer-name, .footer-links, .footer-bottom, .fade-in, .map-embed. These class names are mandatory — do not invent alternatives. Include in the <head> the Google Fonts link: <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">. Respect the color palette precisely: primary steel blue #0f4c81, slate accent #64748b, cool gray background #f5f7fa, text #1a2332, surface white #ffffff, borders #e2e8f0. Write all copy in natural Spanish adapted to the business type, with confident urban tone — polished yet accessible, with global sensibility and local soul. Build a card-forward layout with clear grid structure, alternating section backgrounds (#f5f7fa / white), full-bleed hero with blue overlay, slightly desaturated photography, and a sticky navigation. Use real Unsplash urban imagery. Structure sections in this order: nav, hero, about, services, features, reviews, FAQ, CTA band, contact with map, footer.`,
    cssInject: `/* ===================================================
   URBANO — Contemporary Urban Lifestyle Design System
   =================================================== */

:root {
  --primary: #0f4c81;
  --primary-dark: #0a3a63;
  --accent: #64748b;
  --bg: #f5f7fa;
  --text: #1a2332;
  --text-soft: #475569;
  --surface: #ffffff;
  --border: #e2e8f0;
  --radius-card: 12px;
  --radius-btn: 8px;
  --shadow: 0 4px 16px rgba(15, 76, 129, 0.10);
  --shadow-hover: 0 12px 32px rgba(15, 76, 129, 0.18);
  --font-heading: 'Inter', system-ui, sans-serif;
  --font-body: 'Inter', system-ui, sans-serif;
  --container: 1200px;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html { scroll-behavior: smooth; }

body {
  font-family: var(--font-body);
  font-weight: 400;
  color: var(--text);
  background: var(--surface);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

h1, h2, h3, h4 {
  font-family: var(--font-heading);
  font-weight: 800;
  letter-spacing: -0.025em;
  line-height: 1.15;
  color: var(--text);
}

img { max-width: 100%; display: block; filter: saturate(0.9) contrast(1.02); }

a { color: inherit; text-decoration: none; }

section { padding: 96px 24px; }
section:nth-of-type(even) { background: var(--bg); }

/* ================= NAV ================= */
.nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 40px;
  background: transparent;
  transition: background .3s ease, box-shadow .3s ease, padding .3s ease;
}

.nav.scrolled {
  background: rgba(255, 255, 255, 0.96);
  backdrop-filter: blur(10px);
  box-shadow: 0 2px 12px rgba(15, 76, 129, 0.08);
  padding: 12px 40px;
}

.nav-logo {
  font-family: var(--font-heading);
  font-weight: 800;
  font-size: 1.35rem;
  letter-spacing: -0.03em;
  color: #fff;
  transition: color .3s ease;
}
.nav.scrolled .nav-logo { color: var(--primary); }

.nav-links {
  display: flex;
  gap: 36px;
  list-style: none;
}

.nav-links li a {
  position: relative;
  font-size: 0.95rem;
  font-weight: 600;
  color: #fff;
  padding: 6px 0;
  transition: color .25s ease;
}
.nav.scrolled .nav-links li a { color: var(--text); }

.nav-links li a::after {
  content: '';
  position: absolute;
  left: 0; bottom: 0;
  width: 0; height: 2px;
  background: var(--primary);
  transition: width .3s ease;
}
.nav.scrolled .nav-links li a::after { background: var(--primary); }
.nav-links li a:hover::after,
.nav-links li a.active::after { width: 100%; }

.nav-cta {
  padding: 11px 22px;
  background: var(--primary);
  color: #fff !important;
  border-radius: var(--radius-btn);
  font-weight: 600;
  font-size: 0.9rem;
  transition: transform .25s ease, box-shadow .25s ease, background .25s ease;
}
.nav-cta:hover {
  background: var(--primary-dark);
  transform: translateY(-2px);
  box-shadow: 0 6px 16px rgba(15, 76, 129, 0.35);
}
.nav-cta::after { display: none !important; }

.nav-hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  cursor: pointer;
  background: none;
  border: none;
  padding: 6px;
}
.nav-hamburger span {
  width: 26px; height: 2px;
  background: #fff;
  transition: background .3s ease;
}
.nav.scrolled .nav-hamburger span { background: var(--text); }

/* ================= HERO ================= */
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 120px 24px 80px;
  background-size: cover;
  background-position: center;
  color: #fff;
  overflow: hidden;
}

.hero::before {
  content: '';
  position: absolute; inset: 0;
  background: rgba(15, 76, 129, 0.60);
  z-index: 1;
}

.hero-inner {
  position: relative;
  z-index: 2;
  max-width: 880px;
}

.hero-eyebrow {
  display: inline-block;
  font-size: 0.85rem;
  font-weight: 600;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  padding: 8px 18px;
  border: 1px solid rgba(255,255,255,0.4);
  border-radius: 4px;
  margin-bottom: 28px;
}

.hero h1 {
  font-size: clamp(2.5rem, 6vw, 4.5rem);
  font-weight: 800;
  letter-spacing: -0.03em;
  color: #fff;
  margin-bottom: 24px;
}

.hero-sub {
  font-size: 1.2rem;
  font-weight: 400;
  max-width: 640px;
  margin: 0 auto 40px;
  opacity: 0.92;
}

.hero-btns {
  display: flex;
  gap: 16px;
  justify-content: center;
  flex-wrap: wrap;
}

.hero-btns .btn-primary {
  animation: subtlePulse 2.6s ease-in-out infinite;
}

@keyframes subtlePulse {
  0%, 100% { box-shadow: 0 4px 14px rgba(0,0,0,0.25); }
  50% { box-shadow: 0 4px 22px rgba(255,255,255,0.35); }
}

/* ================= BUTTONS ================= */
.btn-primary, .btn-secondary, .btn-outline, .btn-cream {
  display: inline-block;
  padding: 13px 28px;
  font-family: var(--font-body);
  font-weight: 600;
  font-size: 0.95rem;
  border-radius: var(--radius-btn);
  border: none;
  cursor: pointer;
  transition: all .3s ease;
  letter-spacing: 0.01em;
}

.btn-primary {
  background: var(--primary);
  color: #fff;
}
.btn-primary:hover {
  background: var(--primary-dark);
  transform: translateY(-2px);
  box-shadow: 0 8px 22px rgba(15, 76, 129, 0.4);
}

.btn-secondary {
  background: #fff;
  color: var(--primary);
}
.btn-secondary:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 22px rgba(0,0,0,0.2);
}

.btn-outline {
  background: transparent;
  color: #fff;
  border: 2px solid #fff;
  padding: 11px 26px;
}
.btn-outline:hover {
  background: #fff;
  color: var(--primary);
  transform: translateY(-2px);
}

.btn-primary:focus-visible,
.btn-secondary:focus-visible,
.btn-outline:focus-visible,
.btn-cream:focus-visible {
  outline: 3px solid var(--accent);
  outline-offset: 3px;
}

.btn-primary:active, .btn-secondary:active, .btn-outline:active { transform: translateY(0); }

/* ================= SECTION HEADINGS ================= */
.section-heading {
  text-align: center;
  max-width: 720px;
  margin: 0 auto 64px;
}
.section-heading h2 {
  font-size: clamp(2rem, 4vw, 2.8rem);
  margin-bottom: 16px;
}
.section-sub {
  font-size: 1.1rem;
  color: var(--text-soft);
  font-weight: 400;
}

/* ================= ABOUT ================= */
.about-inner {
  max-width: var(--container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 64px;
  align-items: center;
}

.about-text h2 {
  font-size: clamp(1.8rem, 3.5vw, 2.5rem);
  margin-bottom: 24px;
}
.about-text p {
  color: var(--text-soft);
  margin-bottom: 18px;
  font-size: 1.05rem;
}

.about-highlight {
  display: block;
  padding: 20px 24px;
  margin-top: 28px;
  background: var(--bg);
  border-left: 3px solid var(--primary);
  border-radius: 4px;
  font-weight: 600;
  color: var(--primary);
}

.about-img {
  border-radius: var(--radius-card);
  overflow: hidden;
  box-shadow: var(--shadow);
}
.about-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  aspect-ratio: 4/5;
}

/* ================= CARDS GRID (Services) ================= */
.cards-grid {
  max-width: var(--container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
}

.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow);
  padding: 36px 28px;
  transition: transform .35s ease, box-shadow .35s ease, border-color .35s ease;
}
.card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
  border-color: #cbd5e1;
}

.card-icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 52px; height: 52px;
  background: rgba(15, 76, 129, 0.08);
  color: var(--primary);
  border-radius: 10px;
  font-size: 1.5rem;
  margin-bottom: 20px;
}

.card-title {
  font-size: 1.25rem;
  font-weight: 800;
  margin-bottom: 12px;
  letter-spacing: -0.02em;
}
.card-desc {
  color: var(--text-soft);
  font-size: 0.97rem;
  margin-bottom: 20px;
}
.card-price {
  font-weight: 800;
  color: var(--primary);
  font-size: 1.15rem;
  padding-top: 16px;
  border-top: 1px solid var(--border);
}

/* ================= FEATS GRID ================= */
.feats-grid {
  max-width: var(--container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 32px;
}

.feat {
  display: flex;
  gap: 20px;
  padding: 28px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  transition: transform .3s ease, box-shadow .3s ease;
}
.feat:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
}

.feat-icon {
  flex-shrink: 0;
  width: 48px; height: 48px;
  background: var(--primary);
  color: #fff;
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.3rem;
}

.feat-title {
  font-size: 1.15rem;
  font-weight: 800;
  margin-bottom: 8px;
}
.feat-text {
  color: var(--text-soft);
  font-size: 0.97rem;
}

/* ================= REVIEWS ================= */
.reviews-grid {
  max-width: var(--container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
}

.review-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 32px 28px;
  box-shadow: var(--shadow);
  transition: transform .35s ease, box-shadow .35s ease;
}
.review-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
}

.review-stars {
  color: #f59e0b;
  font-size: 1.05rem;
  margin-bottom: 16px;
  letter-spacing: 2px;
}
.review-text {
  color: var(--text-soft);
  font-style: italic;
  margin-bottom: 20px;
  line-height: 1.7;
}
.review-author {
  font-weight: 600;
  color: var(--primary);
  font-size: 0.95rem;
}

/* ================= FAQ ================= */
.faq-list {
  max-width: 820px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.faq-item {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  overflow: hidden;
  transition: box-shadow .3s ease;
}
.faq-item[open] {
  box-shadow: var(--shadow);
  border-color: var(--primary);
}

.faq-q {
  padding: 22px 26px;
  font-weight: 600;
  font-size: 1.02rem;
  cursor: pointer;
  list-style: none;
  display: flex;
  justify-content: space-between;
  align-items: center;
  color: var(--text);
}
.faq-q::-webkit-details-marker { display: none; }
.faq-q::after {
  content: '+';
  font-size: 1.5rem;
  color: var(--primary);
  font-weight: 400;
  transition: transform .3s ease;
}
.faq-item[open] .faq-q::after { transform: rotate(45deg); }

.faq-a {
  padding: 0 26px 22px;
  color: var(--text-soft);
  line-height: 1.7;
}

/* ================= CTA BAND ================= */
.cta-band {
  background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
  color: #fff;
  text-align: center;
  padding: 88px 24px;
}
.cta-band h2 {
  color: #fff;
  font-size: clamp(1.8rem, 4vw, 2.6rem);
  margin-bottom: 16px;
}
.cta-band p {
  font-size: 1.1rem;
  opacity: 0.92;
  max-width: 600px;
  margin: 0 auto 32px;
}
.cta-band .btn-cream {
  background: #fff;
  color: var(--primary);
}
.cta-band .btn-cream:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 26px rgba(0,0,0,0.25);
  background: #f8fafc;
}

/* ================= CONTACT ================= */
.contact-grid {
  max-width: var(--container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 56px;
}

.contact-info h3 {
  font-size: 1.4rem;
  margin-bottom: 20px;
}
.contact-info p {
  color: var(--text-soft);
  margin-bottom: 14px;
  display: flex;
  align-items: center;
  gap: 12px;
}
.contact-info strong { color: var(--text); font-weight: 600; }

.contact-form {
  background: var(--surface);
  padding: 36px;
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow);
}

.form-group {
  margin-bottom: 20px;
}
.form-group label {
  display: block;
  margin-bottom: 8px;
  font-weight: 600;
  font-size: 0.9rem;
  color: var(--text);
}
.form-group input,
.form-group textarea {
  width: 100%;
  padding: 12px 14px;
  font-family: var(--font-body);
  font-size: 0.97rem;
  color: var(--text);
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: var(--radius-btn);
  transition: border-color .25s ease, box-shadow .25s ease, background .25s ease;
}
.form-group textarea { min-height: 130px; resize: vertical; }
.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--primary);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(15, 76, 129, 0.15);
}

.form-submit {
  width: 100%;
  padding: 14px 28px;
  background: var(--primary);
  color: #fff;
  border: none;
  border-radius: var(--radius-btn);
  font-weight: 600;
  font-size: 1rem;
  cursor: pointer;
  transition: all .3s ease;
}
.form-submit:hover {
  background: var(--primary-dark);
  transform: translateY(-2px);
  box-shadow: 0 8px 22px rgba(15, 76, 129, 0.35);
}

.map-embed {
  grid-column: 1 / -1;
  margin-top: 56px;
  border-radius: var(--radius-card);
  overflow: hidden;
  border: 1px solid var(--border);
  box-shadow: var(--shadow);
}
.map-embed iframe { width: 100%; height: 380px; border: 0; display: block; filter: grayscale(0.3); }

/* ================= FOOTER ================= */
footer {
  background: var(--text);
  color: #cbd5e1;
  padding: 64px 24px 28px;
}
.footer-inner {
  max-width: var(--container);
  margin: 0 auto;
}
.footer-top {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr 1fr;
  gap: 40px;
  padding-bottom: 40px;
  border-bottom: 1px solid rgba(255,255,255,0.1);
}
.footer-brand p { margin-top: 14px; font-size: 0.95rem; line-height: 1.7; }
.footer-name {
  font-family: var(--font-heading);
  font-weight: 800;
  font-size: 1.35rem;
  color: #fff;
  letter-spacing: -0.03em;
}
.footer-links {
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.footer-links a {
  color: #cbd5e1;
  font-size: 0.93rem;
  transition: color .25s ease;
}
.footer-links a:hover { color: #fff; }
.footer-bottom {
  padding-top: 28px;
  text-align: center;
  font-size: 0.88rem;
  color: #94a3b8;
}

/* ================= UTILITIES ================= */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity .6s ease, transform .6s ease;
}
.fade-in.visible {
  opacity: 1;
  transform: none;
}

/* ================= RESPONSIVE ================= */
@media (max-width: 1024px) {
  .cards-grid { grid-template-columns: repeat(2, 1fr); }
  .reviews-grid { grid-template-columns: repeat(2, 1fr); }
  .footer-top { grid-template-columns: 1fr 1fr; }
  .nav { padding: 16px 24px; }
  section { padding: 80px 24px; }
}

@media (max-width: 768px) {
  .nav-links { display: none; }
  .nav-hamburger { display: flex; }
  .nav-cta { display: none; }
  .about-inner { grid-template-columns: 1fr; gap: 40px; }
  .cards-grid { grid-template-columns: 1fr; }
  .feats-grid { grid-template-columns: 1fr; }
  .reviews-grid { grid-template-`,
  },

  natural: {
    name: 'Natural',
    description: 'Orgánico, eco, bienestar natural',
    accentColor: '#5a7a52',
    bgColor: '#f7f3ed',
    textColor: '#2d2318',
    promptDirective: `Generate a complete, semantic HTML5 page for a Spanish local business using the "Natural" design system — an organic, earth-connected aesthetic perfect for yoga studios, organic food shops, herbalists, eco-businesses, and wellness retreats. You MUST use the EXACT class names from the provided vocabulary: .nav, .nav-logo, .nav-links, .nav-cta, .hero, .hero-inner, .hero-eyebrow, .hero-sub, .hero-btns, .btn-primary, .btn-secondary, .btn-outline, .section-heading, .section-sub, .about, .about-inner, .about-text, .about-img, .about-highlight, .cards-grid, .card, .card-icon, .card-title, .card-desc, .card-price, .feats-grid, .feat, .feat-icon, .feat-title, .feat-text, .reviews-grid, .review-card, .review-stars, .review-text, .review-author, .faq-list, .faq-item, .faq-q, .faq-a, .cta-band, .btn-cream, .contact-grid, .contact-info, .contact-form, .form-group, .form-submit, .footer-inner, .footer-top, .footer-brand, .footer-name, .footer-links, .footer-bottom, .fade-in, .map-embed. Include in <head> the Google Fonts link for Libre Baskerville (400,700) and Source Sans 3 (400,600). Follow this palette with EXACT hex values: sage primary #5a7a52, warm earth accent #a67c5b, parchment background #f7f3ed, text #2d2318, soft surface #fdf9f4. The design personality is grounded, authentic, and harmonious with nature — use serif headlines with soft weight, italic eyebrow labels (handwritten feel), pill-shaped buttons, organic cards with subtle earthy borders, botanical SVG leaf dividers between sections, and generous breathing room. The hero needs a warm parchment overlay over a nature image. Write all content in natural, warm Spanish adapted to the business type. Include sections: nav, hero, about, services (cards-grid), features (feats-grid), reviews, FAQ, CTA band, contact with form and map, footer. Add .fade-in on scroll-animated elements.`,
    cssInject: `@import url('https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;700&family=Source+Sans+3:wght@400;600&display=swap');

:root {
  --primary: #5a7a52;
  --primary-dark: #466140;
  --accent: #a67c5b;
  --accent-dark: #8a6647;
  --bg: #f7f3ed;
  --text: #2d2318;
  --text-soft: #5c4f3e;
  --surface: #fdf9f4;
  --border: rgba(90, 122, 82, 0.2);
  --radius-card: 14px;
  --radius-btn: 50px;
  --shadow: 0 3px 16px rgba(90, 122, 82, 0.08);
  --shadow-lg: 0 10px 32px rgba(90, 122, 82, 0.15);
  --font-heading: 'Libre Baskerville', Georgia, serif;
  --font-body: 'Source Sans 3', system-ui, sans-serif;
  --transition: 0.3s ease;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

html { scroll-behavior: smooth; }

body {
  font-family: var(--font-body);
  color: var(--text);
  background: var(--bg);
  line-height: 1.7;
  font-size: 17px;
  -webkit-font-smoothing: antialiased;
}

h1, h2, h3, h4 {
  font-family: var(--font-heading);
  font-weight: 400;
  color: var(--text);
  line-height: 1.25;
  letter-spacing: -0.01em;
}

img { max-width: 100%; display: block; }
a { color: var(--primary); text-decoration: none; transition: color var(--transition); }
a:hover { color: var(--accent); }

section { padding: 100px 24px; position: relative; }
section:nth-child(even) { background: var(--surface); }

.section-heading {
  text-align: center;
  max-width: 760px;
  margin: 0 auto 64px;
}
.section-heading h2 {
  font-size: clamp(2rem, 4vw, 2.75rem);
  margin-bottom: 18px;
}
.section-sub {
  color: var(--text-soft);
  font-size: 1.05rem;
  font-style: italic;
}

/* NAVIGATION */
.nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 48px;
  background: transparent;
  transition: all 0.4s ease;
}
.nav.scrolled {
  background: rgba(253, 249, 244, 0.96);
  backdrop-filter: blur(10px);
  box-shadow: 0 2px 20px rgba(90, 122, 82, 0.08);
  padding: 14px 48px;
}
.nav-logo {
  font-family: var(--font-heading);
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text);
  letter-spacing: 0.02em;
}
.nav-links {
  display: flex;
  gap: 36px;
  list-style: none;
}
.nav-links li a {
  color: var(--text);
  font-weight: 400;
  font-size: 0.98rem;
  position: relative;
  padding: 4px 0;
}
.nav-links li a::after {
  content: '';
  position: absolute;
  bottom: 0; left: 0;
  width: 0; height: 1px;
  background: var(--accent);
  transition: width var(--transition);
}
.nav-links li a:hover::after { width: 100%; }
.nav-cta {
  background: var(--primary);
  color: var(--surface);
  padding: 10px 22px;
  border-radius: var(--radius-btn);
  font-size: 0.92rem;
  transition: all var(--transition);
}
.nav-cta:hover {
  background: var(--primary-dark);
  color: var(--surface);
  transform: translateY(-1px);
}
.nav-hamburger {
  display: none;
  background: none;
  border: none;
  cursor: pointer;
  width: 28px; height: 22px;
  position: relative;
}
.nav-hamburger span {
  display: block;
  height: 2px;
  background: var(--text);
  margin: 5px 0;
  transition: var(--transition);
}

/* HERO */
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 120px 24px 80px;
  background:
    linear-gradient(rgba(247, 243, 237, 0.45), rgba(247, 243, 237, 0.55)),
    url('https://images.unsplash.com/photo-1501854140801-50d01698950b?w=1920') center/cover no-repeat;
  position: relative;
}
.hero-inner {
  max-width: 820px;
  animation: gentleFadeIn 1.2s ease;
}
.hero-eyebrow {
  font-style: italic;
  color: var(--accent);
  font-size: 1.05rem;
  margin-bottom: 18px;
  letter-spacing: 0.03em;
}
.hero h1 {
  font-size: clamp(2.4rem, 5.5vw, 4.2rem);
  font-weight: 400;
  color: var(--text);
  margin-bottom: 24px;
  line-height: 1.15;
}
.hero-sub {
  font-size: 1.2rem;
  color: var(--text-soft);
  margin-bottom: 40px;
  max-width: 620px;
  margin-left: auto;
  margin-right: auto;
}
.hero-btns {
  display: flex;
  gap: 16px;
  justify-content: center;
  flex-wrap: wrap;
}

@keyframes gentleFadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: none; }
}
@keyframes softBreath {
  0%, 100% { box-shadow: 0 4px 16px rgba(90, 122, 82, 0.2); }
  50% { box-shadow: 0 6px 24px rgba(90, 122, 82, 0.35); }
}

/* BUTTONS */
.btn-primary, .btn-secondary, .btn-outline, .btn-cream, .nav-cta, .form-submit {
  display: inline-block;
  font-family: var(--font-body);
  font-weight: 600;
  font-size: 1rem;
  padding: 14px 32px;
  border-radius: var(--radius-btn);
  border: none;
  cursor: pointer;
  transition: all var(--transition);
  text-align: center;
  letter-spacing: 0.02em;
}
.btn-primary {
  background: var(--primary);
  color: var(--surface);
  animation: softBreath 3.5s ease-in-out infinite;
}
.btn-primary:hover {
  background: var(--primary-dark);
  color: var(--surface);
  transform: translateY(-2px);
  animation: none;
}
.btn-primary:active { transform: translateY(0); }
.btn-secondary {
  background: var(--accent);
  color: var(--surface);
}
.btn-secondary:hover {
  background: var(--accent-dark);
  color: var(--surface);
  transform: translateY(-2px);
}
.btn-outline {
  background: transparent;
  color: var(--primary);
  border: 1.5px solid var(--primary);
}
.btn-outline:hover {
  background: var(--primary);
  color: var(--surface);
  transform: translateY(-2px);
}
.btn-cream {
  background: var(--surface);
  color: var(--primary);
}
.btn-cream:hover {
  background: var(--bg);
  color: var(--primary-dark);
  transform: translateY(-2px);
}

/* ABOUT */
.about-inner {
  max-width: 1180px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 72px;
  align-items: center;
}
.about-text h2 {
  font-size: clamp(1.9rem, 3.6vw, 2.6rem);
  margin-bottom: 24px;
}
.about-text p {
  color: var(--text-soft);
  margin-bottom: 18px;
  font-size: 1.05rem;
}
.about-highlight {
  display: inline-block;
  margin-top: 20px;
  padding: 14px 24px;
  background: rgba(166, 124, 91, 0.12);
  border-left: 3px solid var(--accent);
  border-radius: 4px;
  font-style: italic;
  color: var(--accent-dark);
}
.about-img {
  border-radius: var(--radius-card);
  overflow: hidden;
  box-shadow: var(--shadow-lg);
  aspect-ratio: 4/5;
}
.about-img img { width: 100%; height: 100%; object-fit: cover; }

/* CARDS GRID */
.cards-grid {
  max-width: 1180px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
}
.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 36px 30px;
  box-shadow: var(--shadow);
  transition: all 0.4s ease;
  position: relative;
}
.card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
  border-color: rgba(90, 122, 82, 0.35);
}
.card-icon {
  width: 52px; height: 52px;
  display: flex; align-items: center; justify-content: center;
  background: rgba(90, 122, 82, 0.1);
  color: var(--primary);
  border-radius: 50%;
  font-size: 1.5rem;
  margin-bottom: 22px;
}
.card-title {
  font-family: var(--font-heading);
  font-size: 1.35rem;
  font-weight: 700;
  margin-bottom: 12px;
}
.card-desc {
  color: var(--text-soft);
  margin-bottom: 18px;
  font-size: 0.98rem;
}
.card-price {
  font-family: var(--font-heading);
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--accent);
}

/* FEATURES */
.feats-grid {
  max-width: 1080px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 48px;
}
.feat {
  display: flex;
  gap: 22px;
  align-items: flex-start;
  padding: 24px;
  transition: all var(--transition);
  border-radius: var(--radius-card);
}
.feat:hover {
  background: rgba(90, 122, 82, 0.04);
  transform: translateY(-4px);
}
.feat-icon {
  flex-shrink: 0;
  width: 56px; height: 56px;
  background: var(--bg);
  color: var(--accent);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 1.6rem;
  border: 1px solid var(--border);
}
.feat-title {
  font-family: var(--font-heading);
  font-size: 1.25rem;
  font-weight: 700;
  margin-bottom: 8px;
}
.feat-text {
  color: var(--text-soft);
  font-size: 0.98rem;
}

/* REVIEWS */
.reviews-grid {
  max-width: 1180px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
}
.review-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 34px 28px;
  box-shadow: var(--shadow);
  transition: all 0.4s ease;
}
.review-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
}
.review-stars {
  color: var(--accent);
  font-size: 1.05rem;
  margin-bottom: 16px;
  letter-spacing: 2px;
}
.review-text {
  color: var(--text-soft);
  font-style: italic;
  margin-bottom: 20px;
  font-size: 1rem;
  line-height: 1.7;
}
.review-author {
  font-family: var(--font-heading);
  font-weight: 700;
  color: var(--text);
  font-size: 0.98rem;
}

/* FAQ */
.faq-list {
  max-width: 820px;
  margin: 0 auto;
}
.faq-item {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  margin-bottom: 16px;
  padding: 4px 26px;
  transition: all var(--transition);
}
.faq-item[open] {
  box-shadow: var(--shadow);
  border-color: rgba(90, 122, 82, 0.3);
}
.faq-q {
  font-family: var(--font-heading);
  font-size: 1.1rem;
  font-weight: 400;
  padding: 20px 0;
  cursor: pointer;
  list-style: none;
  position: relative;
  padding-right: 36px;
  color: var(--text);
}
.faq-q::-webkit-details-marker { display: none; }
.faq-q::after {
  content: '+';
  position: absolute;
  right: 0; top: 50%;
  transform: translateY(-50%);
  font-size: 1.5rem;
  color: var(--accent);
  transition: transform var(--transition);
}
.faq-item[open] .faq-q::after {
  content: '–';
}
.faq-a {
  padding: 0 0 22px;
  color: var(--text-soft);
  font-size: 1rem;
  line-height: 1.7;
}

/* CTA BAND */
.cta-band {
  background: var(--primary);
  color: var(--surface);
  text-align: center;
  padding: 90px 24px;
}
.cta-band h2 {
  color: var(--surface);
  font-size: clamp(1.9rem, 4vw, 2.8rem);
  margin-bottom: 18px;
}
.cta-band p {
  color: rgba(253, 249, 244, 0.9);
  max-width: 620px;
  margin: 0 auto 34px;
  font-size: 1.1rem;
}

/* CONTACT */
.contact-grid {
  max-width: 1180px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 56px;
}
.contact-info h3 {
  font-family: var(--font-heading);
  font-size: 1.5rem;
  margin-bottom: 16px;
}
.contact-info p {
  color: var(--text-soft);
  margin-bottom: 14px;
}
.contact-form {
  background: var(--surface);
  padding: 40px;
  border-radius: var(--radius-card);
  border: 1px solid var(--border);
  box-shadow: var(--shadow);
}
.form-group { margin-bottom: 20px; }
.form-group label {
  display: block;
  margin-bottom: 8px;
  font-weight: 600;
  color: var(--text);
  font-size: 0.95rem;
}
.form-group input,
.form-group textarea {
  width: 100%;
  padding: 12px 16px;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 10px;
  font-family: var(--font-body);
  font-size: 1rem;
  color: var(--text);
  transition: all var(--transition);
}
.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--accent);
  background: var(--surface);
  box-shadow: 0 0 0 3px rgba(166, 124, 91, 0.15);
}
.form-group textarea {
  resize: vertical;
  min-height: 130px;
}
.form-submit {
  background: var(--primary);
  color: var(--surface);
  width: 100%;
}
.form-submit:hover {
  background: var(--primary-dark);
  transform: translateY(-2px);
}
.map-embed {
  margin-top: 32px;
  border-radius: var(--radius-card);
  overflow: hidden;
  border: 1px solid var(--border);
  box-shadow: var(--shadow);
}
.map-embed iframe {
  width: 100%;
  height: 280px;
  border: 0;
  display: block;
}

/* FOOTER */
footer {
  background: var(--text);
  color: rgba(247, 243, 237, 0.8);
  padding: 72px 24px 24px;
}
.footer-inner {
  max-width: 1180px;
  margin: 0 auto;
}
.footer-top {
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr 1fr;
  gap: 48px;
  padding-bottom: 48px;
  border-bottom: 1px solid rgba(247, 243, 237, 0.1);
}
.footer-brand p {
  margin-top: 14px;
  color: rgba(247, 243, 237, 0.6);
  font-size: 0.95rem;
}
.footer-name {
  font-family: var(--font-heading);
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--bg);
}
.footer-links {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.footer-links a {
  color: rgba(247, 243, 237, 0.7);
  font-size: 0.95rem;
  transition: color var(--transition);
}
.footer-links a:hover { color: var(--bg); }
.footer-bottom {
  padding-top: 24px;
  text-align: center;
  font-size: 0.88rem;
  color: rgba(247, 243, 237, 0.5);
}

/* FADE IN */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}
.fade-in.visible {
  opacity: 1;
  transform: none;
}

/* RESPONSIVE */
@media (max-width: 1024px) {
  .nav { padding: 18px 28px; }
  .nav.scrolled { padding: 12px 28px; }
  section { padding: 80px 20px; }
  .cards-grid { grid-template-columns: repeat(2, 1fr); gap: 24px; }
  .about-inner { gap: 48px; }
  .footer-top { grid-template-columns: 1fr 1fr; gap: 36px; }
}

@media (max-width: 768px) {
  body { font-size: 16px; }
  .nav { padding: 16px 20px; }
  .nav-links {
    display: none;
    position: absolute;
    top: 100%; left: 0; right: 0;
    background: var(--surface);
    flex-direction: column;
    padding: 24px;
    box-shadow: var(--shadow);
  }
  .nav-links.open { display: flex; }
  .nav-hamburger { display: block; }
  .nav-cta { display: none; }
  section { padding: 64px 18px; }
  .hero { padding: 100px 20px 60px; }
  .hero-btns { flex-direction: column; align-items: stretch; }
  .hero-btns a { width: 100%; }
  .about-inner { grid-template-columns: 1fr; gap: 40px; }
  .about-img { aspect-ratio: 4/3; }
  .cards-grid { grid-template-columns: 1fr; gap: 20px; }
  .feats-grid { grid-template-columns: 1fr; gap: 24px; }
  .reviews-grid { grid-template-columns: 1fr; gap: 20px; }
  .contact-grid { grid-template-columns: 1fr; gap: 36px; }
  .contact-form { padding: 28px 22px; }
  .footer-top { grid-template-columns: 1fr; gap: 30px; text-align: center; }
  .section-heading { margin-bottom: 44px; }
  .cta-band { padding: 64px 20px; }
  .feat { flex-direction: column; align-items: flex-start; }
}`,
  },

  nordico: {
    name: 'Nórdico',
    description: 'Escandinavo, funcional, preciso',
    accentColor: '#4a6fa5',
    bgColor: '#f8fafc',
    textColor: '#1e293b',
    promptDirective: `Generate a complete single-page Spanish business website using the "Nórdico" design system — Scandinavian functional minimalism characterised by clarity, precision, and quiet confidence. You MUST use these EXACT class names from the vocabulary without deviation: .nav, .nav-logo, .nav-links, .nav-cta, .nav-hamburger, .hero, .hero-inner, .hero-eyebrow, .hero-sub, .hero-btns, .btn-primary, .btn-secondary, .btn-outline, .section-heading, .section-sub, .about, .about-inner, .about-text, .about-img, .about-highlight, .cards-grid, .card, .card-icon, .card-title, .card-desc, .card-price, .feats-grid, .feat, .feat-icon, .feat-title, .feat-text, .reviews-grid, .review-card, .review-stars, .review-text, .review-author, .faq-list, .faq-item, .faq-q, .faq-a, .cta-band, .contact-grid, .contact-info, .contact-form, .form-group, .form-submit, .footer-inner, .footer-top, .footer-brand, .footer-name, .footer-links, .footer-bottom, .fade-in, .map-embed. Include the Google Font in the <head>: <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;700&display=swap" rel="stylesheet">. Strictly follow the palette: cool white #f8fafc background, blue-gray accent #4a6fa5 used sparingly, slate text #1e293b, light surface #f1f5f9, borders #e2e8f0, and one dark slate section #1e293b for the CTA band. Headings use weight 700 with letter-spacing -0.02em; body uses weight 400 with line-height 1.8. Cards are borderless or with 1px #e2e8f0 border, 8px radius, minimal shadow. Buttons are slightly rounded (8px), no gradients. Hero is two-tone asymmetric with oversized left headline. Write natural, warm Spanish copy adapted to the business type. Avoid decorative flourishes — use only functional icons (SVG or unicode). Maintain generous whitespace and grid-based composition throughout.`,
    cssInject: `@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;700&display=swap');

:root {
  --primary: #1e293b;
  --accent: #4a6fa5;
  --bg: #f8fafc;
  --text: #1e293b;
  --text-muted: #64748b;
  --surface: #f1f5f9;
  --border: #e2e8f0;
  --white: #ffffff;
  --radius-card: 8px;
  --radius-btn: 8px;
  --shadow: 0 2px 8px rgba(0,0,0,.06);
  --shadow-hover: 0 12px 32px rgba(0,0,0,.10);
  --font-heading: 'DM Sans', sans-serif;
  --font-body: 'DM Sans', sans-serif;
  --container: 1200px;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  font-family: var(--font-body);
  font-weight: 400;
  line-height: 1.8;
  color: var(--text);
  background: var(--bg);
  -webkit-font-smoothing: antialiased;
}

img { max-width: 100%; display: block; }

h1, h2, h3, h4 {
  font-family: var(--font-heading);
  font-weight: 700;
  letter-spacing: -0.02em;
  line-height: 1.15;
  color: var(--text);
}

a { color: inherit; text-decoration: none; transition: color .2s ease; }

/* NAV */
.nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.25rem 2.5rem;
  background: rgba(248, 250, 252, 0);
  transition: background .3s ease, box-shadow .3s ease, padding .3s ease;
}
.nav.scrolled {
  background: rgba(248, 250, 252, 0.92);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--border);
  padding: 0.85rem 2.5rem;
}
.nav-logo {
  font-weight: 700;
  font-size: 1.15rem;
  letter-spacing: -0.02em;
  color: var(--text);
}
.nav-links {
  display: flex;
  gap: 2.25rem;
  list-style: none;
}
.nav-links li a {
  font-size: 0.92rem;
  font-weight: 400;
  color: var(--text);
  position: relative;
  padding: 0.25rem 0;
  transition: color .2s ease;
}
.nav-links li a:hover { color: var(--accent); }
.nav-links li a::after {
  content: '';
  position: absolute;
  left: 0; bottom: -2px;
  width: 0; height: 1px;
  background: var(--accent);
  transition: width .25s ease;
}
.nav-links li a:hover::after { width: 100%; }
.nav-cta {
  padding: 0.7rem 1.4rem;
  background: var(--primary);
  color: var(--white) !important;
  border-radius: var(--radius-btn);
  font-size: 0.9rem;
  font-weight: 400;
  transition: background .2s ease, transform .2s ease;
}
.nav-cta:hover { background: #0f172a; transform: translateY(-1px); }
.nav-hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 6px;
}
.nav-hamburger span {
  display: block;
  width: 22px;
  height: 1.5px;
  background: var(--text);
  transition: .3s;
}

/* HERO */
.hero {
  min-height: 100vh;
  display: grid;
  grid-template-columns: 1.1fr 1fr;
  align-items: stretch;
  background: var(--bg);
}
.hero-inner {
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 8rem 5rem 5rem;
  max-width: 720px;
}
.hero-eyebrow {
  font-size: 0.82rem;
  font-weight: 400;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--accent);
  margin-bottom: 1.75rem;
}
.hero h1 {
  font-size: clamp(2.8rem, 5.5vw, 4.75rem);
  font-weight: 700;
  letter-spacing: -0.03em;
  line-height: 1.05;
  margin-bottom: 1.75rem;
}
.hero-sub {
  font-size: 1.1rem;
  font-weight: 400;
  color: var(--text-muted);
  line-height: 1.75;
  margin-bottom: 2.5rem;
  max-width: 520px;
}
.hero-btns {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}
.hero::after {
  content: '';
  background: var(--primary) url('https://images.unsplash.com/photo-1497366216548-37526070297c?w=1200&q=80') center/cover;
  background-blend-mode: luminosity;
  opacity: 1;
}

/* BUTTONS */
.btn-primary, .btn-secondary, .btn-outline {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0.95rem 1.75rem;
  font-family: var(--font-body);
  font-size: 0.95rem;
  font-weight: 400;
  border-radius: var(--radius-btn);
  border: 1px solid transparent;
  cursor: pointer;
  transition: all .25s ease;
  letter-spacing: 0.01em;
}
.btn-primary {
  background: var(--primary);
  color: var(--white);
}
.btn-primary:hover {
  background: #0f172a;
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(30,41,59,.22);
}
.btn-primary:active { transform: translateY(0); }

.btn-secondary {
  background: transparent;
  color: var(--accent);
  border-color: var(--accent);
}
.btn-secondary:hover {
  background: var(--accent);
  color: var(--white);
  transform: translateY(-2px);
}

.btn-outline {
  background: transparent;
  color: var(--text);
  border-color: var(--border);
}
.btn-outline:hover {
  border-color: var(--text);
  background: var(--text);
  color: var(--white);
}

.btn-cream {
  display: inline-flex;
  align-items: center;
  padding: 0.95rem 1.9rem;
  background: var(--white);
  color: var(--primary);
  border-radius: var(--radius-btn);
  font-weight: 400;
  font-size: 0.95rem;
  transition: all .25s ease;
}
.btn-cream:hover {
  background: var(--bg);
  transform: translateY(-2px);
  box-shadow: 0 10px 24px rgba(0,0,0,.2);
}

/* SECTIONS */
section { padding: 7rem 2.5rem; }

.section-heading {
  max-width: var(--container);
  margin: 0 auto 4rem;
  text-align: left;
}
.section-heading h2 {
  font-size: clamp(2rem, 3.5vw, 2.85rem);
  font-weight: 700;
  letter-spacing: -0.025em;
  margin-bottom: 1rem;
  max-width: 780px;
}
.section-sub {
  font-size: 1.05rem;
  color: var(--text-muted);
  font-weight: 400;
  max-width: 640px;
  line-height: 1.75;
}

/* ABOUT */
.about { background: var(--bg); }
.about-inner {
  max-width: var(--container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 5rem;
  align-items: center;
}
.about-text h2 {
  font-size: clamp(2rem, 3.5vw, 2.85rem);
  margin-bottom: 1.75rem;
}
.about-text p {
  color: var(--text-muted);
  margin-bottom: 1.25rem;
  font-size: 1.02rem;
}
.about-highlight {
  display: inline-block;
  margin-top: 1.5rem;
  padding: 1.25rem 1.5rem;
  border-left: 2px solid var(--accent);
  background: var(--surface);
  color: var(--text);
  font-weight: 400;
  border-radius: 0 var(--radius-card) var(--radius-card) 0;
}
.about-img {
  border-radius: var(--radius-card);
  overflow: hidden;
  box-shadow: var(--shadow);
}
.about-img img { width: 100%; height: 100%; object-fit: cover; aspect-ratio: 4/5; }

/* CARDS GRID */
.cards-grid {
  max-width: var(--container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}
.card {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 2.25rem 2rem;
  transition: transform .3s ease, box-shadow .3s ease, border-color .3s ease;
}
.card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
  border-color: transparent;
}
.card-icon {
  width: 44px; height: 44px;
  display: flex; align-items: center; justify-content: center;
  color: var(--accent);
  margin-bottom: 1.75rem;
  font-size: 1.5rem;
}
.card-title {
  font-size: 1.2rem;
  font-weight: 700;
  margin-bottom: 0.85rem;
  letter-spacing: -0.015em;
}
.card-desc {
  color: var(--text-muted);
  font-size: 0.97rem;
  line-height: 1.75;
  margin-bottom: 1.5rem;
}
.card-price {
  font-size: 1.35rem;
  font-weight: 700;
  color: var(--text);
  letter-spacing: -0.02em;
  padding-top: 1rem;
  border-top: 1px solid var(--border);
}

/* FEATURES */
.feats-grid {
  max-width: var(--container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2.5rem 4rem;
}
.feat {
  display: flex;
  gap: 1.5rem;
  padding: 1.5rem 0;
}
.feat-icon {
  flex-shrink: 0;
  width: 48px; height: 48px;
  background: var(--surface);
  color: var(--accent);
  display: flex; align-items: center; justify-content: center;
  border-radius: var(--radius-card);
  font-size: 1.3rem;
}
.feat-title {
  font-size: 1.12rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
}
.feat-text {
  color: var(--text-muted);
  font-size: 0.97rem;
  line-height: 1.75;
}

/* REVIEWS */
.reviews-grid {
  max-width: var(--container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}
.review-card {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 2.25rem 2rem;
  transition: transform .3s ease, box-shadow .3s ease;
}
.review-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-hover);
}
.review-stars {
  color: var(--accent);
  font-size: 0.95rem;
  letter-spacing: 0.15em;
  margin-bottom: 1.25rem;
}
.review-text {
  color: var(--text);
  font-size: 1rem;
  line-height: 1.75;
  margin-bottom: 1.5rem;
  font-weight: 400;
}
.review-author {
  font-size: 0.9rem;
  font-weight: 700;
  color: var(--text);
  letter-spacing: -0.01em;
}

/* FAQ */
.faq-list {
  max-width: 820px;
  margin: 0 auto;
}
.faq-item {
  border-bottom: 1px solid var(--border);
  padding: 0.5rem 0;
}
.faq-q {
  cursor: pointer;
  padding: 1.5rem 2rem 1.5rem 0;
  font-size: 1.05rem;
  font-weight: 400;
  color: var(--text);
  list-style: none;
  position: relative;
  transition: color .2s ease;
}
.faq-q::-webkit-details-marker { display: none; }
.faq-q::after {
  content: '+';
  position: absolute;
  right: 0; top: 50%;
  transform: translateY(-50%);
  font-size: 1.4rem;
  font-weight: 300;
  color: var(--accent);
  transition: transform .25s ease;
}
.faq-item[open] .faq-q::after { transform: translateY(-50%) rotate(45deg); }
.faq-q:hover { color: var(--accent); }
.faq-a {
  padding: 0 2rem 1.5rem 0;
  color: var(--text-muted);
  font-size: 0.98rem;
  line-height: 1.8;
}

/* CTA BAND */
.cta-band {
  background: var(--primary);
  color: var(--white);
  padding: 6rem 2.5rem;
  text-align: center;
}
.cta-band h2 {
  color: var(--white);
  font-size: clamp(2rem, 3.5vw, 2.85rem);
  margin-bottom: 1.25rem;
  max-width: 720px;
  margin-left: auto;
  margin-right: auto;
}
.cta-band p {
  color: rgba(255,255,255,.72);
  font-size: 1.08rem;
  max-width: 580px;
  margin: 0 auto 2.5rem;
  line-height: 1.75;
}

/* CONTACT */
.contact-grid {
  max-width: var(--container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1.2fr;
  gap: 4.5rem;
}
.contact-info {
  font-size: 1rem;
  color: var(--text-muted);
  line-height: 1.9;
}
.contact-info h3 {
  font-size: 1.5rem;
  margin-bottom: 1.5rem;
  color: var(--text);
}
.contact-info p { margin-bottom: 0.6rem; }
.contact-info strong { color: var(--text); font-weight: 700; }

.contact-form {
  background: var(--white);
  padding: 2.5rem;
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
}
.form-group { margin-bottom: 1.5rem; }
.form-group label {
  display: block;
  font-size: 0.85rem;
  font-weight: 400;
  color: var(--text-muted);
  margin-bottom: 0.5rem;
  letter-spacing: 0.02em;
}
.form-group input,
.form-group textarea {
  width: 100%;
  padding: 0.85rem 1rem;
  font-family: var(--font-body);
  font-size: 0.98rem;
  color: var(--text);
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: var(--radius-btn);
  transition: border-color .25s ease, background .25s ease, box-shadow .25s ease;
}
.form-group textarea { min-height: 130px; resize: vertical; }
.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--accent);
  background: var(--white);
  box-shadow: 0 0 0 3px rgba(74,111,165,.12);
}
.form-submit {
  width: 100%;
  padding: 1rem 1.75rem;
  background: var(--primary);
  color: var(--white);
  border: none;
  border-radius: var(--radius-btn);
  font-family: var(--font-body);
  font-size: 0.98rem;
  font-weight: 400;
  cursor: pointer;
  transition: background .25s ease, transform .2s ease;
}
.form-submit:hover { background: #0f172a; transform: translateY(-2px); }
.form-submit:active { transform: translateY(0); }

.map-embed {
  width: 100%;
  height: 400px;
  border: none;
  border-radius: var(--radius-card);
  margin-top: 3rem;
  filter: grayscale(30%) contrast(0.95);
}

/* FOOTER */
footer {
  background: var(--bg);
  border-top: 1px solid var(--border);
  padding: 4rem 2.5rem 2rem;
}
.footer-inner {
  max-width: var(--container);
  margin: 0 auto;
}
.footer-top {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr 1fr;
  gap: 3rem;
  padding-bottom: 3rem;
  border-bottom: 1px solid var(--border);
}
.footer-brand {
  font-size: 0.95rem;
  color: var(--text-muted);
  line-height: 1.75;
  max-width: 320px;
}
.footer-name {
  font-size: 1.15rem;
  font-weight: 700;
  letter-spacing: -0.02em;
  color: var(--text);
  margin-bottom: 1rem;
  display: block;
}
.footer-links {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}
.footer-links a {
  color: var(--text-muted);
  font-size: 0.92rem;
  transition: color .2s ease;
}
.footer-links a:hover { color: var(--accent); }
.footer-bottom {
  padding-top: 2rem;
  text-align: center;
  color: var(--text-muted);
  font-size: 0.85rem;
}

/* FADE-IN */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity .6s ease, transform .6s ease;
}
.fade-in.visible {
  opacity: 1;
  transform: none;
}

/* RESPONSIVE */
@media (max-width: 1024px) {
  .hero { grid-template-columns: 1fr; }
  .hero-inner { padding: 7rem 2.5rem 4rem; }
  .hero::after { min-height: 420px; }
  .cards-grid, .reviews-grid { grid-template-columns: repeat(2, 1fr); }
  .footer-top { grid-template-columns: 1fr 1fr; gap: 2.5rem; }
  section { padding: 5.5rem 2rem; }
}

@media (max-width: 768px) {
  .nav { padding: 1rem 1.25rem; }
  .nav.scrolled { padding: 0.75rem 1.25rem; }
  .nav-links { display: none; }
  .nav-hamburger { display: flex; }
  .hero-inner { padding: 6rem 1.5rem 3rem; }
  .hero h1 { font-size: 2.5rem; }
  .hero-btns { flex-direction: column; align-items: stretch; }
  .hero-btns .btn-primary, .hero-btns .btn-secondary { width: 100%; }
  section { padding: 4.5rem 1.5rem; }
  .about-inner { grid-template-columns: 1fr; gap: 3rem; }
  .cards-grid, .reviews-grid, .feats-grid { grid-template-columns: 1fr; gap: 1.5rem; }
  .contact-grid { grid-template-columns: 1fr; gap: 2.5rem; }
  .contact-form { padding: 1.75rem; }
  .footer-top { grid-template-columns: 1fr; gap: 2rem; }
  .cta-band { padding: 4.5rem 1.5rem; }
  .section-heading { margin-bottom: 2.5rem; }
}

@media (max-width: 480px) {
  .hero h1 { font-size: 2.1rem; }
  .section-heading h2, .about-text h2, .cta-band h2 { font-size: 1.75rem; }
}`,
  },

  festivo: {
    name: 'Festivo',
    description: 'Alegre, celebración, colorido',
    accentColor: '#f4845f',
    bgColor: '#fff8f6',
    textColor: '#3d1a0e',
    promptDirective: `Generate a complete, cheerful Spanish local business website using the Festivo design system. This preset is designed for pastry shops, flower shops, event planners, children's activities, gift shops, confectionery, and celebration venues — the tone must radiate pure joy, warm optimism, and celebration. You MUST use the exact class names from the standardised vocabulary: .nav, .nav-logo, .nav-links, .nav-cta, .hero, .hero-inner, .hero-eyebrow, .hero-sub, .hero-btns, .btn-primary, .btn-secondary, .btn-outline, .section-heading, .section-sub, .about, .about-inner, .about-text, .about-img, .about-highlight, .cards-grid, .card, .card-icon, .card-title, .card-desc, .card-price, .feats-grid, .feat, .feat-icon, .feat-title, .feat-text, .reviews-grid, .review-card, .review-stars, .review-text, .review-author, .faq-list, .faq-item, .faq-q, .faq-a, .cta-band, .btn-cream, .contact-grid, .contact-info, .contact-form, .form-group, .form-submit, .footer-inner, .footer-top, .footer-brand, .footer-name, .footer-links, .footer-bottom, .fade-in, .map-embed. Any deviation breaks the styling. Include in the <head>: <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700;800&display=swap" rel="stylesheet">. Use this exact palette: coral primary #f4845f, warm yellow accent #fbbf24 (for prices and highlights), soft pink background #fff8f6, dark text #3d1a0e, white surfaces #ffffff. Alternate section backgrounds between #fff8f6 and #ffffff. Use extra-rounded pill buttons, 20px rounded cards with a coral top border, yellow rounded badges for prices, and bright warm imagery from Unsplash (pastries, flowers, balloons, celebrations). Write all content in natural, warm Spanish adapted to the business type, with friendly emoji-free but celebratory language. Include hero, about, services (3-6 cards), features, reviews, FAQ, CTA band, contact form with map, and footer.`,
    cssInject: `@import url('https://fonts.googleapis.com/css2?family=Nunito:wght@400;700;800&display=swap');

:root {
  --primary: #f4845f;
  --primary-bright: #ff9776;
  --accent: #fbbf24;
  --accent-soft: #fde68a;
  --bg: #fff8f6;
  --text: #3d1a0e;
  --text-soft: #7a4a3a;
  --surface: #ffffff;
  --radius-card: 20px;
  --radius-btn: 50px;
  --shadow: 0 4px 20px rgba(244,132,95,.15);
  --shadow-lg: 0 12px 36px rgba(244,132,95,.25);
  --shadow-glow: 0 6px 20px rgba(244,132,95,.45);
  --font-heading: 'Nunito', system-ui, sans-serif;
  --font-body: 'Nunito', system-ui, sans-serif;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

html { scroll-behavior: smooth; }

body {
  font-family: var(--font-body);
  font-weight: 400;
  color: var(--text);
  background: var(--bg);
  line-height: 1.65;
  -webkit-font-smoothing: antialiased;
}

h1, h2, h3, h4 {
  font-family: var(--font-heading);
  font-weight: 800;
  line-height: 1.2;
  color: var(--text);
}

img { max-width: 100%; display: block; border-radius: 16px; }
a { color: inherit; text-decoration: none; }

section { padding: 90px 24px; position: relative; }
section:nth-of-type(even) { background: var(--surface); }
section:nth-of-type(odd) { background: var(--bg); }

/* ===== NAVIGATION ===== */
.nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 1000;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 18px 40px;
  background: transparent;
  transition: all .35s ease;
}
.nav.scrolled {
  background: rgba(255,255,255,.96);
  box-shadow: 0 4px 20px rgba(244,132,95,.12);
  padding: 12px 40px;
  backdrop-filter: blur(10px);
}
.nav-logo {
  font-family: var(--font-heading);
  font-weight: 800;
  font-size: 1.5rem;
  color: var(--primary);
  letter-spacing: -.5px;
}
.nav-links {
  display: flex;
  gap: 8px;
  list-style: none;
  align-items: center;
}
.nav-links li a {
  display: inline-block;
  padding: 10px 18px;
  font-weight: 700;
  color: var(--text);
  border-radius: var(--radius-btn);
  transition: all .25s ease;
}
.nav-links li a:hover {
  background: rgba(244,132,95,.12);
  color: var(--primary);
  transform: translateY(-2px);
}
/* On scrolled (white) nav, coral #f4845f has only 2.5:1 on white → use dark text instead */
.nav.scrolled .nav-links li a:hover { color: var(--text); background: rgba(61,26,14,.08); }
.nav-cta {
  background: var(--primary);
  color: #fff !important;
  padding: 12px 24px !important;
  border-radius: var(--radius-btn);
  box-shadow: var(--shadow-glow);
  font-weight: 800 !important;
}
.nav-cta:hover { background: var(--primary-bright); transform: translateY(-2px) scale(1.03); }
.nav-hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 8px;
}
.nav-hamburger span {
  display: block;
  width: 26px; height: 3px;
  background: var(--text);
  border-radius: 3px;
  transition: .3s;
}

/* ===== HERO ===== */
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 120px 24px 80px;
  background:
    linear-gradient(rgba(244,132,95,.15), rgba(244,132,95,.15)),
    linear-gradient(135deg, #ffe8de 0%, #fff3c4 100%);
  position: relative;
  overflow: hidden;
}
.hero::before {
  content: '';
  position: absolute; inset: 0;
  background-image:
    radial-gradient(circle at 15% 25%, rgba(251,191,36,.3) 0 8px, transparent 8px),
    radial-gradient(circle at 85% 75%, rgba(244,132,95,.25) 0 10px, transparent 10px),
    radial-gradient(circle at 70% 20%, rgba(251,191,36,.2) 0 6px, transparent 6px);
  pointer-events: none;
}
.hero-inner {
  max-width: 900px;
  position: relative;
  z-index: 2;
}
.hero-eyebrow {
  display: inline-block;
  background: rgba(255,255,255,.8);
  color: var(--primary);
  font-weight: 800;
  padding: 8px 20px;
  border-radius: var(--radius-btn);
  font-size: .9rem;
  margin-bottom: 24px;
  letter-spacing: .5px;
  text-transform: uppercase;
  box-shadow: var(--shadow);
}
.hero h1 {
  font-size: clamp(2.5rem, 6vw, 4.5rem);
  font-weight: 800;
  margin-bottom: 20px;
  letter-spacing: -1px;
}
.hero-sub {
  font-size: 1.25rem;
  color: var(--text-soft);
  margin-bottom: 36px;
  font-weight: 400;
}
.hero-btns {
  display: flex;
  gap: 16px;
  justify-content: center;
  flex-wrap: wrap;
}

/* ===== BUTTONS ===== */
.btn-primary, .btn-secondary, .btn-outline, .btn-cream, .form-submit {
  display: inline-block;
  font-family: var(--font-heading);
  font-weight: 800;
  font-size: 1rem;
  padding: 16px 34px;
  border-radius: var(--radius-btn);
  border: none;
  cursor: pointer;
  transition: all .3s cubic-bezier(.34,1.56,.64,1);
  text-align: center;
}
.btn-primary {
  background: var(--primary);
  color: #fff;
  box-shadow: var(--shadow-glow);
  animation: pulse 2.4s ease-in-out infinite;
}
.btn-primary:hover {
  background: var(--primary-bright);
  transform: translateY(-3px) scale(1.04);
  box-shadow: 0 10px 28px rgba(244,132,95,.55);
  animation: bounce .6s ease;
}
.btn-secondary {
  background: var(--accent);
  color: var(--text);
  box-shadow: 0 4px 16px rgba(251,191,36,.45);
}
.btn-secondary:hover {
  background: #fcd34d;
  transform: translateY(-3px) scale(1.04);
}
.btn-outline {
  background: transparent;
  color: var(--primary);
  border: 2px solid var(--primary);
}
.btn-outline:hover {
  background: var(--primary);
  color: #fff;
  transform: translateY(-3px);
}
.btn-cream {
  background: #fff;
  color: var(--primary);
  box-shadow: 0 6px 20px rgba(0,0,0,.12);
}
.btn-cream:hover {
  background: var(--accent);
  color: var(--text);
  transform: translateY(-3px) scale(1.04);
}

@keyframes pulse {
  0%,100% { box-shadow: var(--shadow-glow); }
  50% { box-shadow: 0 8px 28px rgba(244,132,95,.65); }
}
@keyframes bounce {
  0%,100% { transform: translateY(-3px) scale(1.04); }
  50% { transform: translateY(-7px) scale(1.06); }
}

/* ===== SECTION HEADINGS ===== */
.section-heading {
  text-align: center;
  max-width: 760px;
  margin: 0 auto 60px;
}
.section-heading h2 {
  font-size: clamp(2rem, 4vw, 3rem);
  margin-bottom: 14px;
}
.section-sub {
  font-size: 1.15rem;
  color: var(--text-soft);
}

/* ===== ABOUT ===== */
.about-inner {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
  align-items: center;
}
.about-text h2 {
  font-size: clamp(2rem, 3.5vw, 2.75rem);
  margin-bottom: 20px;
}
.about-text p {
  color: var(--text-soft);
  margin-bottom: 18px;
  font-size: 1.05rem;
}
.about-img img {
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-lg);
  width: 100%;
}
.about-highlight {
  display: inline-block;
  background: var(--accent);
  color: var(--text);
  padding: 10px 20px;
  border-radius: var(--radius-btn);
  font-weight: 800;
  margin-top: 12px;
  box-shadow: 0 4px 14px rgba(251,191,36,.4);
}

/* ===== CARDS GRID (SERVICES) ===== */
.cards-grid {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 30px;
}
.card {
  background: var(--surface);
  border-radius: var(--radius-card);
  border-top: 3px solid var(--primary);
  padding: 34px 28px;
  box-shadow: var(--shadow);
  transition: all .35s cubic-bezier(.34,1.56,.64,1);
  text-align: center;
}
.card:hover {
  transform: translateY(-6px);
  box-shadow: var(--shadow-lg);
}
.card-icon {
  width: 70px; height: 70px;
  margin: 0 auto 18px;
  background: linear-gradient(135deg, #ffe8de, #fff3c4);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 2rem;
  color: var(--primary);
}
.card-title {
  font-size: 1.35rem;
  font-weight: 800;
  margin-bottom: 10px;
}
.card-desc {
  color: var(--text-soft);
  margin-bottom: 18px;
  font-size: .98rem;
}
.card-price {
  display: inline-block;
  background: var(--accent);
  color: var(--text);
  font-weight: 800;
  padding: 8px 18px;
  border-radius: var(--radius-btn);
  font-size: 1.05rem;
  box-shadow: 0 3px 10px rgba(251,191,36,.35);
}

/* ===== FEATURES GRID ===== */
.feats-grid {
  max-width: 1100px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 28px;
}
.feat {
  background: var(--surface);
  border-radius: var(--radius-card);
  padding: 30px;
  display: flex;
  gap: 20px;
  align-items: flex-start;
  box-shadow: var(--shadow);
  transition: all .35s ease;
}
.feat:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
}
.feat-icon {
  flex-shrink: 0;
  width: 56px; height: 56px;
  background: var(--primary);
  color: #fff;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 1.5rem;
  box-shadow: var(--shadow-glow);
}
.feat-title {
  font-size: 1.2rem;
  font-weight: 800;
  margin-bottom: 6px;
}
.feat-text {
  color: var(--text-soft);
  font-size: .98rem;
}

/* ===== REVIEWS ===== */
.reviews-grid {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
}
.review-card {
  background: var(--surface);
  border-radius: var(--radius-card);
  padding: 30px;
  box-shadow: var(--shadow);
  border-top: 3px solid var(--accent);
  transition: all .35s ease;
}
.review-card:hover {
  transform: translateY(-6px);
  box-shadow: var(--shadow-lg);
}
.review-stars {
  color: var(--accent);
  font-size: 1.15rem;
  margin-bottom: 14px;
  letter-spacing: 2px;
}
.review-text {
  color: var(--text-soft);
  font-style: italic;
  margin-bottom: 18px;
  line-height: 1.7;
}
.review-author {
  font-weight: 800;
  color: var(--text);
}

/* ===== FAQ ===== */
.faq-list {
  max-width: 820px;
  margin: 0 auto;
}
.faq-item {
  background: var(--surface);
  border-radius: var(--radius-card);
  margin-bottom: 14px;
  box-shadow: var(--shadow);
  overflow: hidden;
  transition: all .3s ease;
}
.faq-item[open] { box-shadow: var(--shadow-lg); }
.faq-q {
  padding: 22px 28px;
  font-weight: 800;
  font-size: 1.08rem;
  cursor: pointer;
  list-style: none;
  display: flex;
  justify-content: space-between;
  align-items: center;
  color: var(--text);
}
.faq-q::-webkit-details-marker { display: none; }
.faq-q::after {
  content: '+';
  font-size: 1.6rem;
  color: var(--primary);
  font-weight: 800;
  transition: transform .3s ease;
}
.faq-item[open] .faq-q::after { transform: rotate(45deg); }
.faq-a {
  padding: 0 28px 22px;
  color: var(--text-soft);
  line-height: 1.7;
}

/* ===== CTA BAND ===== */
.cta-band {
  background:
    radial-gradient(circle at 10% 20%, rgba(255,255,255,.22) 0 10px, transparent 10px),
    radial-gradient(circle at 80% 30%, rgba(255,255,255,.18) 0 8px, transparent 8px),
    radial-gradient(circle at 30% 80%, rgba(251,191,36,.35) 0 12px, transparent 12px),
    radial-gradient(circle at 70% 70%, rgba(255,255,255,.2) 0 6px, transparent 6px),
    linear-gradient(135deg, var(--primary) 0%, #ff9776 100%);
  color: #fff;
  text-align: center;
  padding: 90px 24px;
  border-radius: 40px;
  max-width: 1200px;
  margin: 40px auto;
}
.cta-band h2 {
  color: #fff;
  font-size: clamp(2rem, 4vw, 3rem);
  margin-bottom: 14px;
}
.cta-band p {
  font-size: 1.15rem;
  margin-bottom: 32px;
  opacity: .95;
}

/* ===== CONTACT ===== */
.contact-grid {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 50px;
}
.contact-info {
  background: var(--surface);
  padding: 36px;
  border-radius: var(--radius-card);
  box-shadow: var(--shadow);
  border-top: 3px solid var(--primary);
}
.contact-info h3 { margin-bottom: 16px; font-size: 1.4rem; }
.contact-info p { color: var(--text-soft); margin-bottom: 12px; }
.contact-form {
  background: var(--surface);
  padding: 36px;
  border-radius: var(--radius-card);
  box-shadow: var(--shadow);
}
.form-group { margin-bottom: 18px; }
.form-group label {
  display: block;
  font-weight: 700;
  margin-bottom: 6px;
  font-size: .95rem;
}
.form-group input,
.form-group textarea {
  width: 100%;
  padding: 14px 18px;
  border: 2px solid #ffe0d4;
  border-radius: 16px;
  font-family: var(--font-body);
  font-size: 1rem;
  background: var(--bg);
  color: var(--text);
  transition: all .25s ease;
}
.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--accent);
  background: #fff;
  box-shadow: 0 0 0 4px rgba(251,191,36,.2);
}
.form-group textarea { resize: vertical; min-height: 130px; }
.form-submit {
  background: var(--primary);
  color: #fff;
  width: 100%;
  box-shadow: var(--shadow-glow);
}
.form-submit:hover {
  background: var(--primary-bright);
  transform: translateY(-3px) scale(1.02);
}

.map-embed {
  width: 100%;
  height: 320px;
  border: 0;
  border-radius: var(--radius-card);
  margin-top: 20px;
  box-shadow: var(--shadow);
}

/* ===== FOOTER ===== */
footer {
  background: var(--text);
  color: #fff3ea;
  padding: 70px 24px 30px;
}
.footer-inner {
  max-width: 1200px;
  margin: 0 auto;
}
.footer-top {
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr 1fr;
  gap: 40px;
  margin-bottom: 40px;
}
.footer-brand p { opacity: .75; margin-top: 12px; }
.footer-name {
  font-family: var(--font-heading);
  font-weight: 800;
  font-size: 1.5rem;
  color: var(--accent);
}
.footer-links {
  display: flex;
  flex-direction: column;
  gap: 10px;
  list-style: none;
}
.footer-links a {
  color: #fff3ea;
  opacity: .8;
  transition: all .25s ease;
}
.footer-links a:hover {
  color: var(--accent);
  opacity: 1;
  padding-left: 4px;
}
.footer-bottom {
  border-top: 1px solid rgba(255,255,255,.12);
  padding-top: 24px;
  text-align: center;
  opacity: .7;
  font-size: .9rem;
}

/* ===== FADE-IN ===== */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity .6s ease, transform .6s ease;
}
.fade-in.visible {
  opacity: 1;
  transform: none;
}

/* ===== RESPONSIVE ===== */
@media (max-width: 1024px) {
  .cards-grid { grid-template-columns: repeat(2, 1fr); }
  .reviews-grid { grid-template-columns: repeat(2, 1fr); }
  .footer-top { grid-template-columns: 1fr 1fr; }
  section { padding: 70px 24px; }
}
@media (max-width: 768px) {
  .nav { padding: 14px 20px; }
  .nav.scrolled { padding: 10px 20px; }
  .nav-links {
    display: none;
    position: absolute;
    top: 100%; left: 0; right: 0;
    background: #fff;
    flex-direction: column;
    padding: 20px;
    box-shadow: var(--shadow);
    gap: 6px;
  }
  .nav-links.open { display: flex; }
  .nav-hamburger { display: flex; }
  .hero { padding: 110px 20px 60px; }
  .hero-btns { flex-direction: column; align-items: stretch; }
  .about-inner { grid-template-columns: 1fr; gap: 36px; }
  .cards-grid { grid-template-columns: 1fr; }
  .feats-grid { grid-template-columns: 1fr; }
  .reviews-grid { gr`,
  },

  industrial: {
    name: 'Industrial',
    description: 'Crudo, auténtico, masculino',
    accentColor: '#b5451b',
    bgColor: '#141414',
    textColor: '#f0ece4',
    promptDirective: `Generate a complete Spanish-language HTML page for a local business using the INDUSTRIAL design system. You MUST use the EXACT class names from the standardised vocabulary — no variations, no custom class names. Include this Google Fonts link in the <head>: <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">. Follow the industrial color palette strictly: near-black background #141414, rust/oxide accent #b5451b, warm off-white text #f0ece4, medium-dark surface #1f1f1f, muted #8a8277. The aesthetic is raw, gritty, authentic and built to last — perfect for craft breweries, barbershops, tattoo studios, mechanics and artisan workshops. Use Oswald (condensed, uppercase) for ALL headings with wide letter-spacing; use Inter for body text. All corners are sharp (4px max radius). Cards have a 3px rust left-border. Buttons are rectangular, uppercase, with 0.1em letter-spacing. The hero must be a dark full-bleed photo with a heavy black overlay (rgba(0,0,0,0.7)), featuring a condensed uppercase headline and a rust CTA. Sections alternate between #141414 and #1f1f1f, separated by thin rust accent lines. Write all copy in natural Spanish, adapted to the specific business type: mention craft, process, tradition, authenticity. Include sections in this order: nav, hero, about, services (cards-grid), features (feats-grid), reviews, FAQ, CTA band, contact (with map-embed), footer. Add .fade-in class to major blocks for scroll animation. No softness, no pastels, no decorative flourishes — this design is honest and tough.`,
    cssInject: `@import url('https://fonts.googleapis.com/css2?family=Oswald:wght@400;600;700&family=Inter:wght@400;500;600&display=swap');

:root {
  --primary: #b5451b;
  --accent: #b5451b;
  --bg: #141414;
  --surface: #1f1f1f;
  --text: #f0ece4;
  --muted: #8a8277;
  --border: #2a2a2a;
  --radius-card: 4px;
  --radius-btn: 4px;
  --shadow: 0 4px 20px rgba(0,0,0,0.5);
  --shadow-lg: 0 12px 40px rgba(0,0,0,0.7);
  --font-heading: 'Oswald', 'Arial Narrow', sans-serif;
  --font-body: 'Inter', system-ui, sans-serif;
  --steel-texture: repeating-linear-gradient(45deg, rgba(255,255,255,0.015) 0 2px, transparent 2px 8px);
}

* { box-sizing: border-box; margin: 0; padding: 0; }

html { scroll-behavior: smooth; }

body {
  font-family: var(--font-body);
  background: var(--bg);
  color: var(--text);
  line-height: 1.6;
  font-weight: 400;
  overflow-x: hidden;
  background-image: var(--steel-texture);
}

h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-heading);
  font-weight: 600;
  line-height: 1.1;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  color: var(--text);
}

img { max-width: 100%; display: block; }
a { color: inherit; text-decoration: none; }

/* ---------- NAVIGATION ---------- */
.nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.25rem 3rem;
  background: transparent;
  transition: background 0.3s ease, padding 0.3s ease, border-color 0.3s ease;
  border-bottom: 1px solid transparent;
}
.nav.scrolled {
  background: rgba(20,20,20,0.96);
  padding: 0.8rem 3rem;
  border-bottom: 1px solid var(--border);
  box-shadow: var(--shadow);
}
.nav-logo {
  font-family: var(--font-heading);
  font-size: 1.5rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--text);
}
.nav-logo span { color: var(--accent); }
.nav-links {
  display: flex;
  gap: 2.2rem;
  list-style: none;
}
.nav-links li a {
  font-family: var(--font-heading);
  font-size: 0.95rem;
  font-weight: 400;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: var(--text);
  position: relative;
  padding: 0.3rem 0;
  transition: color 0.2s ease;
}
.nav-links li a::after {
  content: '';
  position: absolute;
  left: 0; bottom: -4px;
  width: 0; height: 2px;
  background: var(--accent);
  transition: width 0.25s ease;
}
.nav-links li a:hover { color: var(--accent); }
.nav-links li a:hover::after { width: 100%; }

.nav-cta {
  font-family: var(--font-heading);
  font-size: 0.9rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  padding: 0.7rem 1.4rem;
  background: var(--accent);
  color: var(--text);
  border-radius: var(--radius-btn);
  border: 2px solid var(--accent);
  transition: all 0.2s ease;
}
.nav-cta:hover {
  background: transparent;
  color: var(--accent);
}

.nav-hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 6px;
}
.nav-hamburger span {
  width: 26px; height: 2px;
  background: var(--text);
  transition: all 0.2s ease;
}

/* ---------- BUTTONS ---------- */
.btn-primary, .btn-secondary, .btn-outline, .btn-cream, .form-submit {
  display: inline-block;
  font-family: var(--font-heading);
  font-size: 0.95rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  padding: 1rem 2rem;
  border-radius: var(--radius-btn);
  cursor: pointer;
  border: 2px solid transparent;
  transition: all 0.25s ease;
  text-align: center;
}
.btn-primary {
  background: var(--accent);
  color: var(--text);
  border-color: var(--accent);
}
.btn-primary:hover {
  background: #943814;
  border-color: #943814;
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(181,69,27,0.35);
}
.btn-secondary {
  background: var(--surface);
  color: var(--text);
  border-color: var(--border);
}
.btn-secondary:hover {
  border-color: var(--accent);
  color: var(--accent);
}
.btn-outline {
  background: transparent;
  color: var(--text);
  border: 2px solid var(--accent);
}
.btn-outline:hover {
  background: var(--accent);
  color: var(--text);
}
.btn-cream {
  background: var(--text);
  color: var(--bg);
  border-color: var(--text);
}
.btn-cream:hover {
  background: transparent;
  color: var(--text);
}

/* ---------- HERO ---------- */
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  background: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)),
    url('https://images.unsplash.com/photo-1513128034602-7814ccaddd4e?w=1920') center/cover no-repeat;
  background-color: #0a0a0a;
  text-align: left;
  padding: 6rem 3rem 4rem;
  overflow: hidden;
}
.hero::before {
  content: '';
  position: absolute;
  inset: 0;
  background: var(--steel-texture);
  pointer-events: none;
}
.hero::after {
  content: '';
  position: absolute;
  left: 0; bottom: 0;
  width: 100%; height: 3px;
  background: var(--accent);
}
.hero-inner {
  position: relative;
  z-index: 2;
  max-width: 1100px;
  width: 100%;
}
.hero-eyebrow {
  display: inline-block;
  font-family: var(--font-heading);
  font-size: 0.9rem;
  font-weight: 400;
  text-transform: uppercase;
  letter-spacing: 0.25em;
  color: var(--accent);
  margin-bottom: 1.5rem;
  padding-left: 3rem;
  position: relative;
}
.hero-eyebrow::before {
  content: '';
  position: absolute;
  left: 0; top: 50%;
  width: 2rem; height: 2px;
  background: var(--accent);
}
.hero h1 {
  font-size: clamp(2.8rem, 7vw, 5.5rem);
  font-weight: 700;
  line-height: 0.95;
  letter-spacing: 0.02em;
  margin-bottom: 1.5rem;
  text-transform: uppercase;
}
.hero-sub {
  font-size: 1.15rem;
  color: var(--text);
  max-width: 600px;
  margin-bottom: 2.5rem;
  opacity: 0.9;
}
.hero-btns {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}
.hero-btns .btn-primary {
  animation: forgePulse 2.8s ease-in-out infinite;
}
@keyframes forgePulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(181,69,27,0.5); }
  50% { box-shadow: 0 0 0 12px rgba(181,69,27,0); }
}

/* ---------- SECTIONS ---------- */
section {
  padding: 6rem 3rem;
  position: relative;
}
section:nth-of-type(even) { background: var(--surface); }
section:nth-of-type(odd) { background: var(--bg); }

.section-heading {
  text-align: center;
  max-width: 800px;
  margin: 0 auto 3.5rem;
}
.section-heading h2 {
  font-size: clamp(2rem, 4.5vw, 3.2rem);
  font-weight: 700;
  margin-bottom: 1rem;
  position: relative;
  display: inline-block;
}
.section-heading h2::after {
  content: '';
  display: block;
  width: 60px; height: 3px;
  background: var(--accent);
  margin: 1rem auto 0;
}
.section-sub {
  font-size: 1.05rem;
  color: var(--muted);
  max-width: 620px;
  margin: 0 auto;
}

/* ---------- ABOUT ---------- */
.about-inner {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: center;
  max-width: 1200px;
  margin: 0 auto;
}
.about-text h2 {
  font-size: clamp(1.9rem, 4vw, 2.8rem);
  margin-bottom: 1.5rem;
  font-weight: 700;
}
.about-text p {
  color: var(--muted);
  margin-bottom: 1.2rem;
  font-size: 1.02rem;
}
.about-highlight {
  display: inline-block;
  border-left: 3px solid var(--accent);
  padding: 0.8rem 1.2rem;
  margin-top: 1rem;
  background: rgba(181,69,27,0.08);
  font-family: var(--font-heading);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text);
}
.about-img {
  width: 100%;
  height: 460px;
  overflow: hidden;
  border-radius: var(--radius-card);
  border-left: 3px solid var(--accent);
}
.about-img img { width: 100%; height: 100%; object-fit: cover; display: block; filter: grayscale(0.4) contrast(1.1); }

/* ---------- SERVICES / CARDS ---------- */
.cards-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem;
  max-width: 1200px;
  margin: 0 auto;
}
.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-left: 3px solid var(--accent);
  border-radius: var(--radius-card);
  padding: 2rem 1.8rem;
  transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
  position: relative;
  overflow: hidden;
}
.card::before {
  content: '';
  position: absolute;
  inset: 0;
  background: var(--steel-texture);
  opacity: 0.5;
  pointer-events: none;
}
.card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
  border-color: var(--accent);
}
.card-icon {
  font-size: 2.2rem;
  color: var(--accent);
  margin-bottom: 1.2rem;
  display: inline-block;
}
.card-title {
  font-family: var(--font-heading);
  font-size: 1.4rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 0.8rem;
}
.card-desc {
  color: var(--muted);
  font-size: 0.98rem;
  margin-bottom: 1.2rem;
}
.card-price {
  font-family: var(--font-heading);
  font-size: 1.6rem;
  font-weight: 700;
  color: var(--accent);
  letter-spacing: 0.05em;
}

/* ---------- FEATURES ---------- */
.feats-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1.5rem;
  max-width: 1100px;
  margin: 0 auto;
}
.feat {
  display: flex;
  gap: 1.5rem;
  padding: 1.8rem;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  transition: all 0.25s ease;
}
.feat:hover {
  border-color: var(--accent);
  transform: translateY(-4px);
  box-shadow: var(--shadow);
}
.feat-icon {
  flex-shrink: 0;
  width: 54px; height: 54px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--accent);
  color: var(--text);
  font-size: 1.4rem;
  border-radius: var(--radius-card);
}
.feat-title {
  font-family: var(--font-heading);
  font-size: 1.25rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 0.4rem;
}
.feat-text {
  color: var(--muted);
  font-size: 0.96rem;
}

/* ---------- REVIEWS ---------- */
.reviews-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem;
  max-width: 1200px;
  margin: 0 auto;
}
.review-card {
  background: var(--bg);
  border: 1px solid var(--border);
  border-left: 3px solid var(--accent);
  border-radius: var(--radius-card);
  padding: 2rem;
  transition: all 0.25s ease;
}
.review-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
}
.review-stars {
  color: var(--accent);
  font-size: 1rem;
  letter-spacing: 0.15em;
  margin-bottom: 1rem;
}
.review-text {
  color: var(--text);
  font-size: 1rem;
  font-style: italic;
  margin-bottom: 1.5rem;
  opacity: 0.9;
}
.review-author {
  font-family: var(--font-heading);
  font-size: 1rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--accent);
}

/* ---------- FAQ ---------- */
.faq-list {
  max-width: 820px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 0.8rem;
}
.faq-item {
  background: var(--surface);
  border: 1px solid var(--border);
  border-left: 3px solid var(--accent);
  border-radius: var(--radius-card);
  padding: 1.3rem 1.6rem;
  transition: all 0.2s ease;
}
.faq-item[open] {
  background: #262626;
  border-color: var(--accent);
}
.faq-q {
  font-family: var(--font-heading);
  font-size: 1.1rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  cursor: pointer;
  list-style: none;
  position: relative;
  padding-right: 2rem;
  color: var(--text);
}
.faq-q::-webkit-details-marker { display: none; }
.faq-q::after {
  content: '+';
  position: absolute;
  right: 0; top: 50%;
  transform: translateY(-50%);
  color: var(--accent);
  font-size: 1.6rem;
  font-weight: 400;
  transition: transform 0.25s ease;
}
.faq-item[open] .faq-q::after { transform: translateY(-50%) rotate(45deg); }
.faq-a {
  margin-top: 1rem;
  color: var(--muted);
  font-size: 1rem;
  line-height: 1.7;
}

/* ---------- CTA BAND ---------- */
.cta-band {
  background: var(--accent);
  padding: 5rem 3rem;
  text-align: center;
  position: relative;
  overflow: hidden;
}
.cta-band::before {
  content: '';
  position: absolute;
  inset: 0;
  background: var(--steel-texture), linear-gradient(135deg, rgba(0,0,0,0.25), transparent);
  pointer-events: none;
}
.cta-band h2 {
  font-size: clamp(2rem, 4.5vw, 3rem);
  font-weight: 700;
  color: var(--text);
  margin-bottom: 1rem;
  position: relative;
}
.cta-band p {
  color: var(--text);
  max-width: 600px;
  margin: 0 auto 2rem;
  font-size: 1.1rem;
  opacity: 0.95;
  position: relative;
}
.cta-band .btn-cream { position: relative; }

/* ---------- CONTACT ---------- */
.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1.2fr;
  gap: 3rem;
  max-width: 1200px;
  margin: 0 auto;
}
.contact-info h3 {
  font-size: 1.5rem;
  margin-bottom: 1.2rem;
  font-weight: 600;
}
.contact-info p {
  color: var(--muted);
  margin-bottom: 0.8rem;
  font-size: 1rem;
}
.contact-info strong {
  font-family: var(--font-heading);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text);
  display: inline-block;
  min-width: 90px;
}

.contact-form {
  background: var(--surface);
  border: 1px solid var(--border);
  border-left: 3px solid var(--accent);
  border-radius: var(--radius-card);
  padding: 2.2rem;
}
.form-group {
  margin-bottom: 1.3rem;
}
.form-group label {
  display: block;
  font-family: var(--font-heading);
  font-size: 0.85rem;
  font-weight: 400;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: var(--muted);
  margin-bottom: 0.5rem;
}
.form-group input,
.form-group textarea {
  width: 100%;
  padding: 0.85rem 1rem;
  font-family: var(--font-body);
  font-size: 1rem;
  background: var(--bg);
  color: var(--text);
  border: 1px solid var(--border);
  border-radius: var(--radius-btn);
  transition: border-color 0.2s ease, box-shadow 0.2s ease;
}
.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--accent);
  box-shadow: 0 0 0 3px rgba(181,69,27,0.2);
}
.form-group textarea {
  resize: vertical;
  min-height: 130px;
}
.form-submit {
  width: 100%;
  background: var(--accent);
  color: var(--text);
  border-color: var(--accent);
}
.form-submit:hover {
  background: #943814;
  border-color: #943814;
  transform: translateY(-2px);
}

.map-embed {
  width: 100%;
  height: 320px;
  border: 1px solid var(--border);
  border-left: 3px solid var(--accent);
  border-radius: var(--radius-card);
  margin-top: 2rem;
  filter: grayscale(0.5) contrast(1.1);
}

/* ---------- FOOTER ---------- */
footer {
  background: #0d0d0d;
  padding: 4rem 3rem 1.5rem;
  border-top: 3px solid var(--accent);
}
.footer-inner {
  max-width: 1200px;
  margin: 0 auto;
}
.footer-top {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr 1fr;
  gap: 3rem;
  margin-bottom: 3rem;
}
.footer-brand p {
  color: var(--muted);
  margin-top: 1rem;
  font-size: 0.95rem;
}
.footer-name {
  font-family: var(--font-heading);
  font-size: 1.5rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--text);
}
.footer-name span { color: var(--accent); }
.footer-top h4 {
  font-size`,
  },

}

export const STYLE_PRESET_KEYS = Object.keys(STYLE_PRESETS) as StylePresetKey[]
